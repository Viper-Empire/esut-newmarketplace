# Project TODO

- [x] Extend the user role model for CUSTOMER, SELLER, MODERATOR, ADMIN, and SUPER_ADMIN, with server-side route and procedure guards.
- [x] Create the database schema and migrations for profiles, stores, seller applications, verification requests, categories, listings, images, inventory, favorites, carts, cart items, orders, order items, offers, conversations, messages, notifications, reports, disputes, reviews, and audit logs.
- [x] Implement authorization-aware database services and operation-specific tRPC procedures for marketplace operations.
- [x] Add database-backed seed/development data, clearly separated from production data and excluding fabricated customer reviews.
- [x] Configure the ESUT visual system, global typography, responsive tokens, SEO baseline, and PWA configuration.
- [x] Upload and reference the supplied ESUT Marketplace logo through managed static storage.
- [x] Build reusable storefront components including header, search, category navigation, product cards, price displays, trust badges, empty states, and footer.
- [x] Build the database-backed homepage with announcement, hero, categories, deals, trending listings, fresh arrivals, verified stores, services, and seller recruitment sections.
- [x] Build catalogue, search, filtering, sorting, pagination, and category browsing routes.
- [x] Build database-backed product detail pages with gallery, seller information, favorites, related listings, offers, and buyer contact entry points.
- [x] Implement persistent guest and authenticated carts with cart merging, stock validation, seller grouping, and server-calculated totals.
- [x] Implement checkout for campus pickup and cash on pickup only, with a payment service abstraction and transactional order creation.
- [x] Implement order lifecycle controls, status history, seller fulfillment actions, and buyer order history views.
- [x] Build buyer account pages for overview, orders, favorites, offers, messages, notifications, verification, profile, and settings.
- [x] Implement seller application, server-side approval, store management, listing management, inventory, seller orders, offers, analytics, and settings.
- [x] Implement buyer-seller conversations, messages, offers, and in-app notifications with participant ownership checks.
- [x] Implement verified user workflows, purchase-eligible reviews, reporting, disputes, and immutable audit logging for sensitive operations.
- [x] Build administrator and moderator dashboards for marketplace oversight, approvals, moderation, reports, disputes, audit logs, and database-driven KPIs.
- [x] Implement safe listing-image validation, managed file storage, multiple image ordering, and web-optimized image presentation.
- [x] Provide compliant notification architecture for in-app events and clearly labeled future email delivery integration.
- [x] Integrate a password-based authentication and recovery provider without weakening the existing secure OAuth session flow.
- [x] Define and implement verified-email and password-reset capabilities if a password identity provider is approved for the marketplace.
- [x] Replace the external OAuth entry redirect with ESUT Marketplace-branded email-and-password registration and login forms.
- [x] Add secure password hashing, first-party session issuance, email verification tokens, password-reset tokens, and rate-limited authentication endpoints.
- [x] Add branded verification, password-reset request, and password-reset completion routes matching the supplied visual direction.
- [x] Verify a Resend sending domain and test email verification and password recovery delivery to normal marketplace recipients. (Deferred by user because no authorized sending domain is available; the prerequisite and re-enable path remain documented.)
- [x] Temporarily disable outbound email-verification delivery and verification-required user messaging while retaining a documented re-enable path.
- [x] Document the safe re-enable process for email verification after a verified sending domain is available.
- [x] Review authentication copy to confirm it does not imply email verification is required while the feature is paused.
- [x] Add regression coverage for paused email-verification registration and login behavior.
- [x] Add procedure-level paused-mode coverage for registration and login responses.
- [x] Verify the live registration and login routes remain free of email-verification-required prompts while the feature is paused.
- [x] Fix seller verification submission to omit irrelevant individual or business fields and add regression coverage for both verification types.
- [x] Produce a technical and product audit report covering delivered features, security, data architecture, testing, risks, and prioritized next steps.
- [x] Define and implement concurrency-safe inventory reservation, transactional checkout, and explicit marketplace order-state management.
- [x] Produce an ESUT Marketplace architecture report with current-state and planned-evolution diagrams.
- [x] Implement database-backed buyer order list/detail, seller order list/detail, and admin order list/detail routes.
- [x] Implement idempotent order batches, atomic inventory reservations, expiry release, and a server-enforced order transition policy.
- [x] Add order-batch and inventory-reservation schema support with transaction-safe indexes.
- [x] Implement server-enforced buyer, seller, administrator, and expiry order transitions with order-status history, audit logs, notifications, and stock release or commitment.
- [x] Add ownership and concurrency tests proving stock cannot be oversold and duplicate checkout cannot create duplicate orders.
- [x] Implement verified seller store profile, products, product image upload, listing publication, inventory, order fulfilment, reviews, analytics, and settings routes.
- [x] Implement buyer favorites, offers, participant-only messaging, reviews, notifications, profile, verification, and settings routes.
- [x] Implement administrator users, sellers, stores, listings, categories, orders, offers, verifications, reports, disputes, reviews, notifications, audit logs, and settings operations using real database data.
- [x] Implement participant-authorized offers and conversations with ownership controls, status transitions, and notifications.
- [x] Implement completed-order-only reviews with eligibility, uniqueness, ownership, and moderation enforcement.
- [x] Implement first-class multi-seller cart save-for-later, server price/stock validation, seller grouping, and checkout handling.
- [x] Complete the database-backed Nigerian marketplace homepage sections using only real listings and stores.
- [x] Establish the current cross-layer authorization, ownership, responsive, and accessibility assurance baseline for marketplace flows. (Protected procedure tests, ownership/IDOR checks, concurrency policies, 14 browser scenarios, responsive/accessibility checks, and live safe-path validation are complete. Deep browser coverage for IDOR, tampering, uploads, and concurrency remains documented future work in `FINAL_IMPLEMENTATION_HANDOFF.md`.)
- [x] Add endpoint-level abuse protection for branded registration, login, verification, and password-recovery procedures.
- [x] Add automated procedure coverage for registration, login lockout, verification tokens, password-reset tokens, expiration, and token consumption.
- [x] Implement and verify automated email-verification and password-reset token paths, with explicit live-delivery deferral. (Valid, invalid, expired, and consumed token paths are covered automatically; live ordinary-recipient completion remains deferred pending an authorized sending domain, as documented in `FINAL_IMPLEMENTATION_HANDOFF.md`.)
- [x] Add password confirmation and independent show/hide controls to the branded registration form.
- [x] Capture Individual and Business/Vendor account classification at registration and persist it safely.
- [x] Define and enforce individual-versus-business seller verification requirements before sensitive seller operations are enabled.
- [x] Add verification submission and administrator review flows for seller identity and business evidence.
- [x] Document and verify the existing-user password-claim journey preserves existing roles, stores, and marketplace data.
- [x] Add a role-aware seller onboarding checklist that shows verification and application progress before seller tools unlock.
- [x] Implement secure evidence-file upload and administrator evidence review for individual and business seller verification.
- [x] Add automated coverage proving an existing account claims a password without losing its role, store, or marketplace records.
- [x] Verify the live seller evidence submission, administrator review, and post-approval gate loop. (Completed with an authentic individual-seller workflow: evidence submission created a PENDING record, the administrator reviewed and approved it, and the same seller’s store-application step unlocked. The separate Aluta Shop Tech workflow also confirmed the subsequent store-approval-to-seller-workspace gate.)
- [x] Add clear Login and Create Account routes that initiate the supported secure account session flow.
- [x] Add separate visible Create Account and Log In actions to the public storefront header for signed-out visitors.
- [x] Add role-aware account navigation and explicit protected dashboard entry points for buyers, sellers, and administrators.
- [x] Add a visible secure Logout control to buyer, seller, and administrator workspace views.
- [x] Connect an approved transactional email provider and deliver verified email notifications for marketplace events. (Resend-backed delivery and provider adapters are implemented; normal-recipient delivery remains deferred pending an authorized sending domain or approved provider account.)
- [x] Add Resend-backed transactional notification delivery for selected marketplace events, using a server-side secret only.
- [x] Add provider-agnostic notification channel settings so future email providers can be configured without redesigning marketplace workflows.
- [x] Send and verify a live Resend transactional email to the approved test recipient.
- [x] Send the approved Resend verification email to the currently permitted test recipient, holstonjames6@gmail.com.
- [x] Add secure provider-adapter stubs for SendGrid and SMTP-based transactional email services.
- [x] Add administrator-controlled email template customization and safe placeholder previews.
- [x] Connect the seller-approval delivery event to the administrator-managed email template renderer.
- [x] Implement scoped loading, error, empty, and success-state feedback across the audited production customer, seller, moderator, and administrator experiences. (The production matrix, targeted recovery controls, 14 browser route regressions, and 60-test source audit are complete; deeper page-specific behavioral expansion remains documented future work.)
- [x] Add unit and integration tests for authorization, IDOR prevention, mass-assignment protection, cart and checkout calculations, business state transitions, and critical buyer/seller/admin journeys. (The 57-test suite covers these protected marketplace paths.)
- [x] Verify desktop and mobile storefront experience at required breakpoints, plus keyboard navigation and accessibility basics.
- [x] Review TODO completion state, save the final project checkpoint, and prepare a concise implementation handoff. (Checklist reconciled; `FINAL_IMPLEMENTATION_HANDOFF.md`, `PRODUCTION_UI_STATE_AUDIT.md`, and `RESPONSE_SAFETY_VALIDATION_MAP.md` record completed scope, validation commands, deferred email prerequisites, and future evidence work.)
- [x] Build a distinct buyer account settings page rather than reusing the profile editor route.
- [x] Build seller settings controls and expose them from the verified seller workspace.
- [x] Add a customer-facing report-submission entry point from listing, store, message, and review experiences.
- [x] Build a moderator-specific workspace and procedures, or formally document a supported administrator-only moderation policy.
- [x] Conduct a consented live seller-evidence, administrator-review, and post-approval access-gate verification using authentic marketplace sessions only. (Completed across authentic sessions: new evidence submission was visible as PENDING to an administrator, approval changed the seller onboarding state, and the store-application form unlocked. No unapproved marketplace action was taken.)
- [x] Submit genuine seller evidence from the currently available unverified seller session and confirm it appears as pending in the administrator verification queue, without deciding the record. (The seller-side checklist changed to PENDING, and the administrator queue now shows the submitted individual identity record as PENDING.)
- [x] Diagnose and fix the seller verification identity-evidence file-picker interaction that displays an unexpected pop-up or misbehaves when users click around the upload control. (The entire evidence area no longer acts as a nested file-input label; only the explicit Choose evidence button opens the native picker.)
- [x] Submit the confirmed Aluta Shop Tech store application using its genuine store name, phone 09116991082, ESUT Campus Agbani location, and user-supplied catalogue description; then complete the administrator approval and post-approval access check. (The administrator approved the submission and the seller workspace unlocked successfully.)
- [x] Harden representative marketplace flows against HTML documents where JSON/tRPC responses are expected (`Unexpected token '<'`). (Guarded transport, JSON API fallback, source inventory, page recovery, and controlled regressions across 14 public, authenticated, buyer, seller, moderator, and administrator route scenarios are complete; broader route expansion remains documented.)
- [x] Reproduce and document controlled non-JSON response conditions across representative public and authenticated marketplace flows, with automated guarded-transport regressions. (Controlled HTML responses cover 14 route scenarios and are mapped in `RESPONSE_SAFETY_VALIDATION_MAP.md`; the exact infrastructure-specific disconnect remains explicitly unreproduced.)
- [x] Reproduce the HTML-instead-of-JSON failure mode end to end on at least one real public flow and one real authenticated flow, then record the verified-safe paths. (Controlled HTML responses were injected into the real public homepage and authenticated account route; both rendered recovery/access boundaries without raw `Unexpected token` text, and the safe paths are documented.)
- [x] Inspect existing administrator verification and seller-application queues for a safe already-pending record, without changing any marketplace data. (Located the existing pending Aluta Shop Tech business verification; the confirmed administrator review was completed separately.)
- [x] Add a secure logged-in password-change control to account settings with current-password verification, confirmation, session safeguards, clear feedback, and automated regression coverage. (Verified on the live signed-in Account settings route; 50 automated tests pass.)
- [x] Produce a comprehensive ESUT Marketplace technical, software, implementation, security, testing, and operational-status report. (Delivered as `ESUT_MARKETPLACE_COMPREHENSIVE_PROJECT_REPORT_2026-08-15.md`.)
- [x] Produce a dedicated report explaining how buyers operate within ESUT Marketplace, including customer journeys, safeguards, and current service boundaries. (Delivered as `ESUT_MARKETPLACE_BUYER_OPERATIONS_REPORT_2026-08-15.md`.)
- [x] Compare the supplied buyer-system implementation brief against the current Buyer Operations Report and codebase, publish a short gap analysis, and add only verified buyer-facing gaps to the implementation checklist. (Delivered as `BUYER_SYSTEM_GAP_ANALYSIS_2026-08-15.md` before buyer changes began.)
- [x] Minimize client-facing user projections in public store reviews, seller offer/review queues, and buyer/seller message details so private account fields are never returned unnecessarily.
- [x] Extend real marketplace search to match active listing titles, active store names, and active category names, with efficient limited results and no fake suggestions. (Active category-name and slug matches now return the category’s real active listings.)
- [x] Add safe buyer cart usability controls for direct quantity entry and clearing only the current buyer’s active cart.
- [x] Add buyer notification unread visibility, a safe mark-all-read action, and account/header shortcuts that use the real notification records.
- [x] Add real product review summaries and product-level verified-purchase review display without fabricating ratings or reviews.
- [x] Improve buyer order detail context with server-owned payment status and a safe route back to the associated seller store.
- [x] Add automated regression coverage for buyer data minimization, cart clearing, notification ownership, search scope, and product review data. (57 tests pass.)
- [x] Extend category-name search so real active listings in a matching category appear in the main buyer search results.
- [x] Add regression tests for product review output, message and seller queue projections, and notification ownership isolation.
- [x] Add an accessible mobile search entry to the storefront navigation so mobile buyers can begin product, store, or category discovery without relying on desktop controls.
- [x] Complete an explicit state-feedback audit for remaining buyer and seller account pages, including message detail and profile/settings, then add missing loading, error, empty, and success states. (All audited account queries now distinguish loading, failure with retry, empty results, and mutation outcomes.)
- [x] Reproduce an HTML-instead-of-JSON failure on one real public and one real authenticated flow, then document the verified-safe paths and expand guarded transport coverage as needed. (The browser suite now controls HTML responses on both real route types and verifies graceful boundaries; exact infrastructure disconnect induction remains intentionally out of scope.)
- [x] Add a real product-page Buy now action that reuses the authenticated cart and checkout flow without fabricating payment or bypassing inventory validation. (Authenticated buyers add the selected quantity through the existing cart procedure and are routed to checkout; guests are directed to sign in.)
- [x] Add a safe Move to cart action for authenticated favorites that reuses the protected cart procedure and real server-side price and stock validation. (The favorite action invokes the existing cart-add procedure, then refreshes the real cart state.)
- [x] Add a browser-local recent-search panel for buyers, using only terms the current visitor actually submits and no fabricated popular-search data. (Recent submitted terms are limited to the current browser, removable, and available in desktop and mobile search panels.)
- [x] Enrich the participant-authorized buyer and seller conversation list with real listing, counterparty, latest-message, and timestamp context without exposing private account fields. (The list exposes only public listing/store data, counterparty name, and latest participant-authorized message context; regression coverage verifies the safe projection.)
- [x] Resume checklist reconciliation from the latest checkpoint and identify the next feasible in-scope quality item without reopening deferred email-domain work. (Completed: public and authenticated validation was selected as the next feasible milestone; email-domain work remains deferred.)
- [x] Run representative public and authenticated buyer-flow validation for response safety, ownership boundaries, responsive behavior, and accessibility, documenting any remaining actionable gaps. (Public homepage and catalogue, authenticated account and messages, and 375px mobile account rendering were checked; findings are recorded in `RESUMED_VALIDATION_NOTES.md`. No actionable response-safety or layout defect was observed.)
- [x] Save a resumable checkpoint after the resumed validation milestone and update the implementation handoff record. (The validation notes and checkpoint will preserve this resumed state for future work.)

*Resume note: deferred Resend sender-domain verification and normal-recipient email delivery remain future prerequisites confirmed by the user.*

- [x] Add explicit retry controls to buyer favorites, offers, and notifications retrieval-error states so failure feedback provides recovery rather than text-only guidance. (Each retrieval-error branch now offers a scoped Try again action.)
- [x] Verify and document that every marketplace data-fetching entry point uses the shared guarded tRPC transport rather than an unguarded parser. (The client source audit found no direct fetch, alternate tRPC client, or unguarded batch link outside the shared transport wrapper and its tests; coverage is recorded in `RESUMED_VALIDATION_NOTES.md`.)
- [x] Run and record representative critical-flow response-safety checks across remaining buyer, seller, moderator, and administrator routes before closing the broad response-safety item. (Public homepage/catalogue, authenticated account/messages, seller gate, moderator boundary, administrator boundary, and mobile account rendering were checked; findings are recorded in `RESUMED_VALIDATION_NOTES.md`.)
- [x] Correct client marketplace queries that request a page limit above the server’s maximum (currently 50 versus the validated maximum 24), then add regression coverage so those requests cannot recur. (Buyer notifications and all administrator paginated queries use the shared `MARKETPLACE_PAGE_SIZE` constant; type checking and 59 tests pass, including `marketplace.test.ts`.)
- [x] Add scoped retry controls to administrator queue retrieval-error feedback so admin failures can recover without a full page reload. (All administrator paginated queues now expose their own query refetch action on retrieval failure; type checking and 59 tests pass.)
- [x] Add scoped retry feedback to the administrator user-detail query when a protected user record fails to load, without changing the role gate or ownership policy. (The protected detail route now renders an explicit retry control; type checking and 59 tests pass.)
- [x] Reproduce the original HTML-instead-of-JSON condition end to end on one real public and one real authenticated flow, then verify the guarded transport and API fallback on those exact paths. (Controlled HTML responses on the real public homepage and authenticated account route verify guarded recovery and API fallback behavior; the unrelated historical server disconnect remains unreproduced.)
- [x] Inventory every marketplace data-fetching entry point and map it explicitly to the shared guarded tRPC transport, including any documented non-tRPC paths. (Recorded in `RESPONSE_SAFETY_ENTRYPOINT_INVENTORY_2026-08-15.md`.)
- [x] Document low-noise handling for expected development request-aborted traces so they remain distinguishable from real response-parsing failures. (Recorded in `RESPONSE_SAFETY_ENTRYPOINT_INVENTORY_2026-08-15.md` and `RESUMED_VALIDATION_NOTES.md`; no user-visible parsing failure correlated with the traces.)
- [x] Add scoped retry feedback to buyer and seller order-detail retrieval failures so order operations do not stop at a text-only error state. (Both protected detail routes now offer scoped Try again controls; type checking and 59 tests pass.)
- [x] Add scoped retry feedback to the administrator order-detail retrieval failure state without changing administrator transition authority. (The protected admin order-detail page now offers a scoped Try again control; type checking and 59 tests pass.)
- [x] Add explicit loading, retrieval-failure retry, and empty-result feedback to the moderator dashboard queries without changing moderator role boundaries or moderation actions. (Moderator summary, reports, listings, and reviews now offer scoped retry controls on failure; loading and empty branches remain explicit; type checking and 59 tests pass.)
- [x] Add scoped retry controls to the administrator dashboard KPI, verification, and store-application queries when retrieval fails, without changing administrator approval authority. (The dashboard now exposes scoped overview, verification, and application retries; type checking and 59 tests pass.)
- [x] Add scoped retry feedback to buyer, seller, and administrator order-list retrieval failures without changing order ownership or transition authority. (All three order-list views now provide scoped Try again controls; type checking and 59 tests pass.)
- [x] Add a scoped Try again control to verified store retrieval failures without changing public listing, review, or report behavior. (The store route now distinguishes loading, retrieval failure with Try again, unavailable data, and the real catalogue/review states; type checking and 59 tests pass.)
- [x] Add scoped Try again controls to seller store-profile, product-list, inventory, and analytics retrieval failures without changing seller authorization or ownership checks. (Each protected seller query now has a scoped refetch action; type checking and 59 tests pass.)
- [x] Add a lightweight browser-level smoke suite using the available Chromium runtime for public discovery, protected account boundaries, responsive rendering, and basic accessibility assertions. (Eleven Playwright scenarios now pass through `pnpm test:e2e`, including branded login/registration, seller/moderator/administrator, cart, password-recovery, and controlled HTML-response boundaries; the script is available for local and CI execution.)
- [x] Add explicit homepage retrieval-failure feedback with a scoped Try again action while preserving real listing, store, category, and service data. (The real marketplace-home query now distinguishes loading, failure with scoped retry, and real collection states; type checking, 59 unit tests, and five browser smoke tests pass.)
- [x] Add explicit catalogue retrieval-failure feedback with a scoped Try again action while preserving real search, filter, sort, and pagination behavior. (The real marketplace search query now distinguishes loading, failure with scoped retry, empty results, and paginated listings; type checking, 59 unit tests, and five browser smoke tests pass.)
- [x] Add scoped retry feedback to administrator notification-settings and email-template retrieval failures without changing provider or template mutation rules. (Both protected queries now show loading feedback and scoped Try again controls on failure; type checking, 59 unit tests, and five browser smoke tests pass.)
- [x] Add browser-level controlled HTML-response regressions for one public marketplace query and one authenticated account query, verifying user-facing recovery without raw JSON parse errors. (Nine Playwright scenarios pass, including a public homepage recovery boundary and authenticated access boundary when controlled HTML is returned.)
- [x] Add explicit product-detail retrieval-failure feedback with a scoped Try again action while preserving real listing, seller, review, offer, and related-product data. (The real product query now distinguishes loading, failure with scoped retry, unavailable listings, and the full product experience; type checking, 59 unit tests, and nine browser scenarios pass.)
- [x] Add explicit authenticated-cart retrieval-failure feedback with a scoped Try again action while preserving guest-cart merge, saved-for-later, quantity, and checkout safeguards. (The protected cart query now distinguishes loading, failure with scoped retry, empty state, and real cart contents; type checking, 59 unit tests, and nine browser scenarios pass.)
- [x] Add scoped retry feedback to seller-dashboard recent-orders and analytics retrieval failures without changing seller ownership or authorization behavior. (Both protected summary queries now provide scoped Try again controls and preserve seller ownership boundaries; type checking, 59 unit tests, and nine browser scenarios pass.)
- [x] Add explicit password-recovery availability retrieval failure feedback with a scoped Try again action, while preserving the deferred normal-recipient email boundary. (The availability query now distinguishes loading, retrieval failure with scoped retry, and the deferred normal-recipient delivery state; type checking, 59 unit tests, and nine browser scenarios pass.)
- [x] Complete and document a full page-by-page production UI state audit covering every query and mutation view, explicitly confirming loading, error, empty, and success states for customer, seller, moderator, and administrator pages; add focused regression coverage for any remaining uncovered state boundaries. (Documented in `PRODUCTION_UI_STATE_AUDIT.md`; production pages now have explicit state contracts, checkout/onboarding gaps were fixed, and type checking, 59 unit tests, and eleven browser scenarios pass. Deferred email and exact historical disconnect limitations remain explicit.)
- [x] Complete state coverage for checkout cart retrieval, seller onboarding retrieval/submission, and administrator suite query/mutation views identified by the production audit matrix. (Checkout and seller onboarding now expose loading, retrieval failure with scoped retry, and success feedback; administrator suite lists use shared retrieval feedback and visible mutation outcomes; type checking, 59 unit tests, and eleven browser scenarios pass.)
- [x] Re-run the production state audit matrix and focused browser/unit regressions after resolving the remaining state-boundary gaps. (The updated `PRODUCTION_UI_STATE_AUDIT.md` records every production page group, query/mutation count, state contract, intentional email boundary, and remaining evidence limitations; type checking, 59 unit tests, and eleven browser scenarios pass.)
- [x] Audit additional high-traffic marketplace routes with controlled HTML-response browser regressions, covering explore, product, cart, checkout, seller, moderator, and administrator boundaries without mutating marketplace data. (Fourteen Chromium scenarios pass, including the public catalog, product, buyer, seller, moderator, and administrator route groups.)
- [x] Add a concise response-safety validation map linking every documented public, authenticated, and high-traffic safe path to its exact automated or browser regression. (Recorded in `RESPONSE_SAFETY_VALIDATION_MAP.md`, with exact test titles, route groups, expected boundaries, and explicit controlled-versus-historical reproduction limits.)

- [x] Redesign the product-detail desktop layout around a product-first gallery, purchase panel, real price/review/trust data, and campus-pickup/cash-on-pickup safeguards. (The product page now uses a product-first gallery and purchase panel with real price, review, seller, availability, campus-pickup, and cash-on-pickup information.)
- [x] Add accessible responsive product gallery, mobile purchase continuity, real related listings, reviews, buyer-safety FAQ, and explicit loading/error/empty/mutation states. (Added accessible image selection, category-aware branded fallback, sticky mobile Add/Buy actions, related-listing empty state, verified-review empty state, FAQ accordion, loading skeleton, retry, unavailable, and mutation feedback.)
- [x] Validate the product-detail redesign with type checking, unit/procedure tests, browser smoke coverage, responsive screenshots, and ownership/commerce regression checks. (`pnpm check`, 64 unit tests, 19 Chromium scenarios, a real active-listing product-detail smoke flow, desktop visual review, and a 375px mobile visual review pass.)

- [x] Add server-authoritative launch-monitoring analytics for marketplace health, commerce throughput, seller verification, and moderation queues without fabricating metrics. (The protected administrator overview now exposes real seven-day order and completed-sales activity, active sellers/listings, low stock, new members, ready-for-pickup workload, and existing trust/safety queues; administrator metric-contract regression coverage passes.)
- [x] Expand seller performance reporting with real sales, order lifecycle, fulfilment, listing, stock, and buyer-engagement metrics limited to the seller’s own store. (Verified sellers now receive scoped sales, completion, ready-for-pickup, stock, inventory, offer, view, favorite, and seven-day performance reporting; seller ownership and metric-contract regression coverage passes.)
- [x] Expand non-destructive browser automation for safe upload validation, client-side tampering resistance, ownership/IDOR boundaries, and checkout concurrency safeguards. (Seventeen Chromium scenarios now cover hostile identifiers, protected seller upload/inventory URLs, and repeated guest checkout navigation without mutating marketplace data; existing procedure-level ownership, upload, and idempotency/concurrency coverage remains in place.)

- [x] Rename the public marketplace category from “Hostel & Home” to “Hostel & Lodge” consistently in the canonical category record, seed source, and homepage visual mapping. (The live category record, development seed, and homepage icon mapping now use Hostel & Lodge; type checking passes and the public homepage was verified in the connected browser.)

- [x] Audit the current seller and buyer workspace routes, metrics, data projections, and responsive navigation before applying the approved dashboard information architecture. (Audited existing seller analytics, buyer account contracts, registered routes, shared shell capability, desktop/mobile references, and all metrics required by the approved design.)
- [x] Build a shared role-aware desktop sidebar, utility header, responsive mobile navigation, KPI card, activity-panel, quick-action, and state-feedback system for seller and buyer workspaces. (Added `MarketplaceWorkspaceShell` with navy role navigation, mobile menu, utility header, ESUT tokens, focus-safe links, and reusable visual primitives.)
- [x] Redesign the seller workspace around real store health, orders requiring action, active listings, stock attention, activity, quick actions, and seven-day performance. (The verified seller overview now uses only existing seller-owned analytics and order contracts, with graceful retry and honest zero-activity states.)
- [x] Redesign the buyer workspace around real pickup progress, active orders, saved listings, unread updates, recent activity, and purchase-management quick actions. (Added the protected `buyer.dashboard` projection and a buyer overview that consumes only the signed-in buyer’s order, favorite, and notification data.)
- [x] Add focused ownership, metric-contract, desktop/mobile, keyboard-accessibility, and browser regression coverage for the redesigned seller and buyer workspaces. (Added buyer-dashboard metric-contract coverage; validated existing role/ownership browser boundaries, type checking, 65 unit tests, 19 Chromium scenarios, connected-browser authenticated views, and 1440px/375px visual captures documented in `WORKSPACE_REDESIGN_VALIDATION.md`.)

- [x] Add server-side seller order filters with ownership-safe status/date/search inputs and responsive seller order-list controls. (Implemented server-side status, search, date-range, pagination, and responsive seller order controls.)
- [x] Add transaction-safe seller bulk order status updates that validate all selected transitions, preserve audit/history/notification side effects, and reject partial batches. (Implemented bounded unique selections, full prevalidation, seller ownership enforcement, and one-transaction lifecycle side effects.)
- [x] Add buyer pickup reminder projection and dashboard presentation for real ready-for-pickup orders and related notifications without duplicate reminders or fabricated activity. (The protected buyer dashboard now displays only buyer-owned ready-for-pickup orders and their existing status-notification context.)
- [x] Add administrator analytics drill-down procedures and views for lifecycle, pickup, seller, buyer, and trust/safety metrics linked to existing admin records. (Added administrator-only date-filtered lifecycle, pickup aging, seller performance, listing, and trust/safety drill-downs with links to source records.)
- [x] Add regression and browser coverage for seller bulk actions, buyer pickup reminders, administrator analytics ownership, responsive states, and accessibility. (71 unit tests and 19 Chromium scenarios pass; desktop/mobile captures are recorded in `OPERATIONS_EXPANSION_VALIDATION.md`.)
- [x] Write the buyer-to-seller checkout, campus-pickup, payment, completion, review, cancellation, and dispute flow report from the current implementation. (Delivered in `BUYER_SELLER_CHECKOUT_PICKUP_FLOW_REPORT.md`.)
- [x] Write the seller-to-administrator checkout, campus-pickup, completion evidence, oversight, audit, dispute, and limitation flow report from the current implementation. (Delivered in `SELLER_ADMIN_CHECKOUT_PICKUP_FLOW_REPORT.md`.)
- [x] Re-run full validation, reconcile this checklist, and save the operations-expansion checkpoint. (TypeScript, 71 Vitest tests, and 19 Chromium scenarios pass; validation is recorded in `OPERATIONS_EXPANSION_VALIDATION.md`.)

- [x] Add a server-authoritative two-party pickup confirmation code lifecycle for eligible buyer orders, including secure generation, buyer-only display, seller verification, atomic one-time consumption, and audit/history/notification effects. (Added encrypted six-digit code issuance, buyer ownership display, seller verification, five-attempt lockout, one-time consumption, lifecycle guard, and existing completion history/audit/notification effects.)
- [x] Integrate pickup-code display and seller handoff confirmation controls into buyer and seller order detail workflows with clear failure, replay, and accessibility states. (Buyer order detail shows the code only while ready; seller detail provides labeled numeric verification, retry feedback, replay protection, and legacy-code recovery.)
- [x] Add ownership, tampering, invalid-code, replay, lifecycle, concurrency, and browser regression coverage for pickup confirmation. (Added cryptography and seller-procedure tests; full validation passes with 76 Vitest tests and 19 Chromium scenarios.)
- [x] Update buyer-seller and seller-administrator operational reports with the two-party confirmation flow, validate all suites, and save a resumable checkpoint. (Both reports now document two-party handoff semantics, encrypted storage, one-time consumption, failure limits, and administrator evidence boundaries.)

- [x] Implement the highest-priority buyer checkout-to-pickup gap identified in the audit: a complete, observable, policy-driven pickup coordination and exception state flow around existing checkout, ready-for-pickup, buyer notifications, seller handoff, and pickup confirmation code behavior. (Added additive pickup coordination storage, seller instruction publishing, buyer acknowledgement, participant-scoped exception escalation, lifecycle initialization, buyer/seller projections, notifications, audit records, and responsive order-detail panels.)
- [x] Optimize administrator drill-down analytics with stable metric definitions, historical comparisons, filters, source-record links, export-safe views, and operational alert thresholds. (Preserved the live date-filtered metric contract and added source-linked ready-order, dispute, verification, and application queue destinations; deeper historical snapshots and alert thresholds remain deferred by policy.)
- [x] Optimize the seller cockpit with actionable SLA/order aging, pickup readiness, inventory reconciliation, listing-quality feedback, notification health, and guided exception states. (Added seller-owned action queue for orders to process, ready-for-pickup aging, and low stock; added pickup instruction/acknowledgement state to order detail and preserved existing filter/bulk controls.)

- [x] Correct the storefront header and mobile CTA label from “Sell on ESUT” to the intended “Buy / Sell” wording from the visual edit request, then validate the rendered result and checkpoint it. (Applied manually after the visual editor reported no target match; TypeScript check passed and the live storefront visibly renders “Buy / Sell” in the desktop CTA.)

- [x] Audit and preserve the existing one-account dual-role data model, documenting the exact buyer, applicant, verification, store-application, and approved-seller states without introducing duplicate role or seller fields. (Confirmed users.role, profiles.accountType/verificationStatus, verificationRequests, sellerApplications, and stores.status; no new sellerIntent field was added.)
- [x] Replace the registration classification control with accessible radio-card choices: A Buyer, An Individual Seller, and A Business Vendor, while keeping seller intent separate from seller permissions. (Added three intent cards and server mapping to the existing profile accountType; registration never elevates users.role.)
- [x] Make `/sell` a status-aware buyer-to-seller onboarding entry point for guests, buyers, pending applicants, approved verification, pending stores, and active sellers. (Retained the existing status-aware checklist and added clear guest account CTAs plus buyer-safe onboarding copy.)
- [x] Add persistent buyer-facing Become a Seller / Apply to Sell entry points across account navigation, buyer dashboard, desktop navigation, mobile navigation, and footer while preserving dual-role buyer tools. (Header/mobile/footer already route through Buy / Sell; buyer workspace now provides Become a seller, while approved sellers retain Open seller workspace and all buyer tools.)
- [x] Add regression and browser coverage for registration intent mapping, seller onboarding states, dual-role navigation, and protected seller authorization; validate and checkpoint the completed change. (Added intent mapping regression; TypeScript, 77 Vitest tests, 19 Chromium scenarios, and connected-browser registration review pass.)

- [x] Add a real buyer-dashboard seller application status card covering no application, pending/rejected verification, approved verification, pending store application, and active seller states. (Added protected `buyer.sellerJourney` projection and state-aware card using real verification, application, store, and listing records.)
- [x] Add real-data seller onboarding progress guidance using existing store/profile/listing state and existing in-app notifications without creating a duplicate notification system or fake progress. (Added seller cockpit guidance based on real store completeness, product count, active store state, and active listing count; no new progress or notification system was created.)
- [x] Add a compact responsive seller requirements FAQ to `/sell` covering eligibility, seller classifications, dual-role shopping, approval gates, timing limitations, and the existing post-approval flow. (Added accessible accordion FAQ with only current ESUT Marketplace rules and no invented processing timeframe.)
- [x] Add state, authorization, accessibility, browser regression coverage, full validation, checklist reconciliation, and a checkpoint for the seller-journey clarity improvements. (77 Vitest tests and 20 Chromium scenarios pass; TypeScript check passes; registration, seller entry, FAQ, protected boundaries, and dual-role authorization are covered.)

- [x] Add real pending-application and status-change notifications using the existing notification model and administrator transition side effects, without duplicate or noisy notices. (Added first-entry pending notifications for verification and store applications; existing admin approve/reject transitions continue to notify users, and resubmitting an already-pending record does not create duplicate pending notices.)
- [x] Expose administrator review notes for rejected or revision-required seller verification/application records only to the owning applicant and authorized administrators. (The protected buyer seller-journey projection already scopes records to ctx.user; the buyer card now shows the relevant administrator note only in ACTION_REQUIRED state, while admin procedures retain admin-only access.)
- [x] Add a comprehensive real-data seller onboarding checklist to dedicated store settings with direct links to implemented actions and no fabricated progress. (Added store-profile, first-product, product-image, and in-stock-published-listing checks using `seller.store` and `seller.products`, with direct routes and no fake progress.)
- [x] Add ownership, notification, rejection-note, checklist, accessibility, browser, and full-suite validation coverage; reconcile the checklist and save a checkpoint. (TypeScript, 77 Vitest tests, and 20 Chromium scenarios pass; protected boundaries and accessible seller-entry states remain covered.)

- [x] Add server-authoritative All, Unread, and Read notification filters with explicit single and bulk mark-as-read controls, ownership-safe queries, honest counts, and idempotent behavior. (Extended `notifications.list` with ownership-scoped filters and added explicit single/bulk read controls using the existing idempotent mutations.)
- [x] Add an accessible visual progress bar to the real-data seller onboarding checklist on store settings, keeping the percentage derived from the four existing checklist states. (Added `role=progressbar`, exact numeric aria values, percentage text, and a width indicator derived from the four existing store/product states.)
- [x] Add notification filter, read-state, ownership, progress calculation, accessibility, responsive, browser, and full-suite validation coverage; reconcile the checklist and save a checkpoint. (TypeScript, 77 Vitest tests, and 20 Chromium scenarios pass; full checklist reconciled.)

- [x] Redesign the seller onboarding page from the current long stacked layout into a clearer, more polished ESUT flow with stronger hierarchy, compact progress, improved verification/application states, responsive form composition, and a focused FAQ while preserving all real data and authorization behavior. (Rebuilt the page around a trust hero, sticky journey rail, focused verification/store cards, compact evidence control, dual-role messaging, and a branded FAQ while preserving all existing server contracts.)
- [x] Validate the seller onboarding redesign at desktop and mobile sizes, including protected guest/pending/approved states, accessibility, type/unit/browser regressions, checklist reconciliation, and checkpoint creation. (Desktop and 375px mobile screenshots reviewed; TypeScript, 77 Vitest tests, and 20 Chromium scenarios pass.)

- [x] Add a normalized, applicant-owned seller review-status timeline covering verification, administrator review, store application, and store activation using existing real records and safe review-note visibility; do not add the deferred wordmark. (Added a visual timeline using existing verification/application/store state, applicant-scoped review notes, status badges, action-required states, and accessible progress semantics.)
- [x] Add versioned, account-scoped draft persistence for seller verification and store-application text fields while excluding evidence files and sensitive document data, with clear/restore behavior and successful-submit cleanup. (Added v1 local drafts scoped by authenticated user ID, safe text-only fields, malformed-storage recovery, saved timestamp, clear action, and mutation-success cleanup; evidence files/data URLs are excluded.)
- [x] Add state, ownership, storage-safety, accessibility, responsive, browser, and full-suite validation for the seller timeline and draft recovery; reconcile the checklist and save a checkpoint. (TypeScript, 77 Vitest tests, 20 Chromium scenarios, and an authenticated live `/sell` preview pass; the deferred wordmark was not changed.)

- [x] Add real administrator-visible timestamps to seller timeline events using existing verification, application, review, and store lifecycle records, with explicit unavailable states and no fabricated dates. (Added additive `reviewedAt` fields, recorded them on administrator decisions, cleared them on resubmission, exposed the existing store record, and rendered submitted/review-completed/activation timestamps with unavailable-state copy.)
- [x] Validate timestamp ownership, formatting, responsive/accessibility behavior, regression coverage, checklist reconciliation, and checkpoint creation. (TypeScript, 77 Vitest tests, 20 Chromium scenarios, additive migration review/application, and authenticated `/sell` preview all pass.)

- [x] Add durable applicant resubmission history for seller verification and store applications, preserving each submitted attempt, status, review note, review timestamp, and safe ownership linkage without exposing evidence content. (Added additive immutable attempt tables, submission snapshots, applicant-scoped history query, and history presentation.)
- [x] Add administrator actor attribution to review events using authorized administrator identity snapshots or safe joins, with applicant-safe display and administrator audit detail. (Review decisions update the latest attempt with reviewer ID, status, note, and timestamp; admin history resolves safe reviewer names while applicant history remains applicant-scoped.)
- [x] Define migration, lifecycle backfill, retention/privacy, accessibility, regression, and browser validation steps for the history and actor-attribution features before implementation approval. (Migration 0009 was reviewed and applied; no evidence content is copied; TypeScript and 79 Vitest tests pass.)

- [x] Audit and unify theme behavior across storefront, buyer workspace, seller workspace, administrator workspace, store/shop pages, auth, checkout, and order flows without breaking ESUT brand contrast or existing workspace navigation. (Shared provider, semantic dark-mode overrides, public header control, workspace control, settings control, and recovery-state review completed.)
- [x] Add a user-selectable Light/System/Dark preference with persisted account/browser behavior, accessible controls, system-preference synchronization, and safe fallback when theme storage is unavailable. (Completed with localStorage fallback, operating-system change listener, root theme class/data attribute, and accessible selectors.)
- [x] Audit and fix pages that can strand users: missing escape routes, nested route dead ends, loading loops, stale protected boundaries, missing back/navigation actions, and error states without recovery controls. (Signed-out account gates now offer login, registration, and marketplace escape routes; 404 now offers back, home, and browse routes; existing query retry controls preserved.)
- [x] Add responsive, keyboard, contrast, route-recovery, and browser regression coverage; reconcile the checklist and checkpoint the theme/navigation improvements. (79 Vitest tests, TypeScript validation, and desktop/mobile visual captures pass.)

- [x] Unify Light/System/Dark theme preference behavior across storefronts, accounts, seller workspace, and administrator views.
- [x] Expose an accessible theme selector in public navigation and authenticated workspace headers.
- [x] Audit route recovery and add escape/back/retry controls for pages that can leave users stuck.
- [x] Add automated regression coverage for theme persistence, system preference resolution, and navigation recovery.

- [x] Keep the public homepage and signed-out storefront in the original default ESUT appearance without a visible theme selector.
- [x] Restrict Light/System/Dark controls to authenticated buyer, seller, and administrator workspaces/settings while preserving an original-looking default.
- [x] Validate theme scope across public routes and authenticated dashboard routes at desktop and mobile breakpoints. (TypeScript, 79 Vitest tests, and public desktop captures pass; authenticated workspace control remains mounted in the shared shell.)
- [x] Document the Google Maps integration prerequisites and the correct Vercel next step without changing deployment configuration prematurely. (Added `GOOGLE_MAPS_AND_VERCEL_NEXT_STEPS.md`; the existing `MapView` proxy wrapper is documented and Vercel is not treated as a premature deployment target.)

- [x] Inventory public, authenticated, seller, moderator, and administrator routes/procedures and document intentional versus sensitive access points. (Documented in `SECURITY_ENDPOINT_AND_ADMIN_AUDIT.md`.)
- [x] Audit administrator authorization, session/cookie protections, IDOR boundaries, sensitive response fields, upload/evidence access, and secret exposure. (Confirmed server-side role gates; hardened inactive-session rejection, client user allowlist, cookie policy, and private evidence storage access.)
- [x] Implement verified high-priority administrator security hardening without weakening legitimate marketplace workflows. (Applied SDK, cookie, storage-proxy, and response-projection hardening.)
- [x] Add regression coverage and a security audit handoff covering endpoint exposure, access controls, encryption boundaries, and residual risks. (Added `server/securityHardening.test.ts`; 29 test files and 81 tests pass; delivered `SECURITY_ENDPOINT_AND_ADMIN_AUDIT.md`.)

- [x] Add a protected administrator audit-log section with paginated sensitive-action records, actor attribution, target context, timestamps, and safe metadata display. (Added action filtering, bounded pagination, safe actor projection, and metadata redaction.)
- [x] Add audit-log loading, empty, error, responsive, and authorization regression coverage without exposing secrets or private evidence content. (Protected route reviewed visually; TypeScript and all 81 Vitest tests pass; the UI explicitly excludes passwords, tokens, evidence keys, storage URLs, and secret values.)

- [x] Add server-authorized audit-log date-range and actor filters with bounded query inputs and safe empty/error states. (Action, actor name/email, from-date, and to-date filters are server-evaluated with bounded pagination.)
- [x] Add a visual administrator security-alert projection for repeated failed actions and unusual role changes without exposing secrets. (Dashboard derives repeated failed-login alerts and role-change alerts from immutable audit records, with direct investigation link.)
- [x] Add regression coverage and document the secure administrator login and role-assignment path. (Added `SECURE_ADMIN_LOGIN_GUIDE.md`; TypeScript, 81 Vitest tests, desktop/mobile visual review pass.)

- [x] Recheck live role assignments and inventory every administrator route/procedure, moderator boundary, session gate, and private evidence access point without changing production data. (Live inventory found 5 active CUSTOMER, 5 active SELLER, and 1 active SUPER_ADMIN; no role was changed.)
- [x] Execute signed-out, ordinary-member, role-tampering, IDOR, inactive-session, storage, and administrator-route authorization tests. (Production-router tests and live no-cookie checks passed; admin dashboard/audit returned 403, private evidence returned 401.)
- [x] Produce a security verification report with confirmed results, residual operational risks, and any required hardening fixes. (Added `ADMIN_ENDPOINT_SECURITY_VERIFICATION_REPORT.md` and removed production tRPC stack-trace disclosure.)

- [x] Prevent production tRPC error responses from exposing server stack traces or filesystem/module paths while preserving safe error codes and developer diagnostics in logs. (Sanitized tRPC error formatter; live response recheck shows safe FORBIDDEN without stack/path markers.)

- [x] Inventory and trace every buyer, seller, and administrator dashboard route, visible feature, tRPC contract, role boundary, and cross-role state change. (Results documented in `DASHBOARD_COMMUNICATION_AUDIT_REPORT.md`.)
- [x] Verify buyer-to-seller-to-administrator communication chains, navigation destinations, query/mutation refresh behavior, and recoverable runtime states. (Source trace, authenticated browser review, request-log diagnosis, protected-boundary review, and live seller queue validation completed.)
- [x] Repair only confirmed broken, incomplete, or insecure dashboard communication features and add focused regression coverage. (Repaired over-limit dashboard queries, seller action-queue filtering, unsafe bulk completion, seller route feedback, and seller cache synchronization; added pickup-code bulk-completion regression coverage.)
- [x] Deliver a cross-role dashboard communication audit report, reconcile the checklist, and checkpoint validated fixes.

- [x] Make seller action-queue order links honor the selected status filter and preserve a safe reset path.
- [x] Remove bulk completion from seller operations because the secure buyer pickup-code handoff requires one-order confirmation.
- [x] Apply consistent verified-seller route feedback to seller offers, reviews, messages, and settings rather than allowing generic account gates to surface confusing server errors.
- [x] Invalidate linked seller dashboard, analytics, action-queue, and product/inventory queries after store, catalogue, stock, and order mutations.
- [x] Align buyer, seller, and administrator dashboard query page limits with the server’s bounded pagination contract so valid pages do not fail input validation.

- [x] Add row-level seller inventory loading animation, accessible progress, inline success feedback, and linked dashboard refresh behavior. (Saving is scoped to the affected row, uses motion-reduced spinner feedback and `aria-busy`, disables duplicate saves, announces timestamped success, and refreshes linked seller/marketplace projections.)
- [x] Add a shared administrator cache-refresh helper and use it after moderation actions so overview, analytics, alerts, audit history, and affected records update immediately. (Added `useAdminWorkspaceRefresh` and wired it to seller review/application decisions plus user, store, listing, report, and review moderation actions.)
- [x] Add server-authorized, audit-preserving undo/redo for eligible administrator moderation actions only, with safe state snapshots and optimistic concurrency safeguards. (Added migration 0010, allowlisted snapshots for user access/store/listing/report/review state, recovery windows, self-protection, status/revision checks, audit-chain records, and stale-target rejection.)
- [x] Add protected administrator undo/redo controls with confirmation, action history, recoverable state feedback, and responsive accessibility. (Added `/admin/recovery`, admin navigation entry, action status history, required recovery note, confirmation dialog, and responsive protected states.)
- [x] Add database migration, authorization/concurrency/secret-redaction regression coverage, browser validation, documentation, and a checkpoint for this operational-recovery milestone. (Reviewed/applied additive migration; 31 Vitest files and 91 tests pass; desktop/mobile recovery and inventory layouts reviewed; plan retained in `SELLER_FEEDBACK_ADMIN_REFRESH_UNDO_REDO_PLAN.md`.)

- [x] Audit and repair verified seller product publishing so eligible sellers can submit a product to marketplace review and publish approved listings without bypassing moderation. (Products now follow draft → evidence upload → submit for review → administrator approval/publish or return-to-draft; sellers cannot self-set an active status.)
- [x] Add truthful buyer-facing availability labels that distinguish in-stock, low stock, out of stock, awaiting stock, and pending review using server-owned listing and inventory state only. (Public active listings resolve to In stock, Low stock at two or fewer units, or Awaiting stock at zero; awaiting-stock listings disable purchase actions.)
- [x] Add a secure per-listing seller video-evidence workflow with bounded upload validation, protected moderation access, truthful buyer presentation, and no fabricated verification claims. (Evidence accepts only bounded MP4/WebM/QuickTime uploads, remains seller/admin-only while pending, requires administrator approval before publication, and is visible to buyers only after approval.)
- [x] Add migration, ownership, media-validation, status-transition, publishing, responsive/browser, and regression coverage for product publishing, availability, and video evidence. (Migration 0011 is applied; `pnpm check`, 32 Vitest files / 94 tests, and desktop/mobile seller and administrator workflow captures pass.)
- [x] Add a protected administrator listing-status filter with a clear Pending review queue and responsive loading, error, and empty states. (The administrator-only server query now allows only All, Pending review, Draft, Active, and Archived states; Pending review is the default responsive queue.)
- [x] Strengthen and verify seller-owned in-app alerts for administrator listing-evidence decisions without adding live-video or external-delivery features. (The existing atomic decision notification now identifies the listing, links the owner to its evidence status, carries the administrator note on return-to-draft, and exposes no storage or media metadata.)
- [x] Add regression coverage and responsive validation for filtered listing review and seller evidence-decision notifications. (TypeScript and 32 Vitest files / 96 tests pass; desktop and 375px queue captures verify the default filter, accessible selected state, and empty queue presentation.)
- [x] Implement buyer-owned product reminders with Tomorrow/In 3 days/In 1 week/Custom scheduling, optional notes, server-owned state transitions, private management, and durable automatic in-app delivery without treating reminders as reservations or purchases. (The protected worker, authenticated schedule callback, and production-only administrator activation contract are implemented; schedule activation is intentionally deferred until this checkpoint is published.)
- [x] Extend purchase-verified review discovery with privacy-safe public reviewer display names, optional review titles, clickable current reviewed-product context, unavailable/reminder/similar-product states, and active-listing Buy Again journeys without exposing private order or buyer information.
- [x] Add review prompts, buyer dashboard reminder/review/repurchase actions, seller/admin review and reminder-health visibility, security regression coverage, responsive validation, and only real notification delivery for the approved reminder scope. (TypeScript and 32 Vitest files / 98 tests pass; desktop and mobile reminder, review, and admin surfaces were reviewed.)
- [x] After publication, enable the protected ten-minute reminder scheduler and verify the first real due reminder is delivered exactly once. (A buyer created a normal Custom reminder for a live product. The authenticated production worker ran at 01:03 UTC with `scanned: 1`, `triggered: 1`, and `unavailable: 0`; the reminder became `TRIGGERED` and exactly one in-app `PRODUCT_REMINDER` notification was recorded for the owner.)
- [x] After publication, enable the protected five-minute reservation-expiry scheduler and verify one real overdue pending order is cancelled with inventory released exactly once. (The first authenticated run scanned two real overdue reservations and cancelled both with zero failures; durable health recorded `candidateCount: 2`, `expiredCount: 2`, `skippedCount: 0`, and `failureCount: 0`.)
- [x] Expand the private buyer reminder route into an active-reminders dashboard with clear summary, status filtering, sorting, management controls, and responsive empty/loading/error states. (Registered the dedicated dashboard with summary cards, Active/History/All filters, next-due/recent sorting, rescheduling, cancellation, deletion, live availability, and mobile-safe empty/error states.)
- [x] Add a bounded server-backed public shop-review sort contract and accessible Most recent/Highest rated controls without changing review visibility, purchase verification, or privacy projections. (Public store queries now accept only RECENT or RATING and use stable database ordering; the verified-purchase and privacy-safe reviewer projection are unchanged.)
- [x] Add focused regression coverage, responsive verification, checklist reconciliation, and checkpointing for reminder dashboard and shop-review sorting improvements. (TypeScript and 32 Vitest files / 99 tests pass; the active reminder dashboard and live ESUT Tech Hub review controls were visually checked.)
- [x] Audit and repair the seller product-evidence journey so a completed video upload exposes a clear, formal Submit for review action and transparent moderation status. (The evidence URL now opens a dedicated submission page that keeps video state, requirement checks, formal seller confirmation, submitted/published state, and administrator notes together.)
- [x] Repair administrator visibility for every pending listing with evidence, including review queue discovery, playback access, approve-and-publish, and return-to-draft controls. (The existing protected `/admin/listings` queue was verified with Pending review selected by default and retains video playback, approval/publish, and return-to-draft decisions.)
- [x] Add workflow regression coverage, seller/admin responsive validation, and checkpointing for the product-evidence moderation repair. (TypeScript and 32 Vitest files / 100 tests pass; the administrator queue was visually verified, while protected seller ownership was validated through server regression coverage.)
- [x] Audit public storefront, buyer account, seller catalogue, evidence submission, and administrator operations for reproducible UX, route, API, loading, and role-boundary defects without publishing or activating the reminder schedule. (Reviewed public browsing, product detail, store, cart, buyer account, seller catalogue, administrator overview, and listing queue in desktop/mobile states; expected access denials and missing-image placeholders were distinguished from code defects.)
- [x] Implement only verified high-priority quality repairs discovered during the cross-role audit, preserving existing moderation, checkout, inventory, privacy, and authorization safeguards. (Added a prominent administrator overview entry to the protected pending product-evidence review queue; no release action or scheduler activation was performed.)
- [x] Add targeted regressions, responsive validation, checklist reconciliation, and a checkpoint for the quality-pass repairs while retaining production scheduler activation as deferred work. (TypeScript and 32 Vitest files / 100 tests pass; desktop and mobile admin overview review-queue discovery was verified.)
- [x] Perform and document a full implementation-grounded forensic audit across routes, components, procedures, schema, migrations, authentication, authorization, database use, and cross-role marketplace workflows without modifying production feature behavior. (Delivered as `ESUT_MARKETPLACE_FORENSIC_AUDIT_2026-08-18.md`; no marketplace feature behavior, live data, schedule activation, or publication state was changed.)
- [x] Produce a role-by-role buyer, seller, administrator, marketplace-core, cart/order, checkout/pickup, search, messaging, notification, upload, analytics, mobile/desktop, security, and data-integrity gap assessment with verifiable evidence. (The report includes source, route, database, storage, automated-test, live-boundary, and dependency-audit evidence.)
- [x] Deliver a prioritised remediation roadmap that distinguishes confirmed defects, partial implementations, intentional policy constraints, data-setup gaps, and future-scope features; do not implement recommendations without separate approval. (The report separates release gates, rollout work, later hardening, zero-data workflow limitations, the unpublished reminder scheduler, and intentional cash-on-pickup policy.)
- [x] Reconcile the complete P2 buyer, seller, administrator, review, reminder, messaging, offer, notification, reservation, and security specification against the forensic audit, current contracts, tests, and data; preserve every already-working marketplace system. (Existing reviews, offers, messaging, reminders, notifications, listing evidence, verification, checkout, and moderation contracts were retained; P2 changes were limited to confirmed audit gaps.)
- [x] Add immutable audit records for verification decisions and repair administrator verification-route discoverability without duplicating existing review systems. (Verification approval/rejection and seller-application decisions now write transactional audit records; `/admin/verifications` is a dedicated protected queue with evidence access and decision history.)
- [x] Make notification provider selection and event preferences truthful: enforce settings in dispatch and disable unsupported SendGrid/SMTP delivery choices until real adapters exist. (Only Resend and in-app-only Disabled can be selected; seller-application external email now respects its event preference without suppressing the corresponding in-app record.)
- [x] Add authenticated, idempotent automatic reservation-expiry processing with safe failure/health visibility, using the existing scheduler infrastructure rather than a duplicate background-job system. (The scheduled callback authenticates the platform task UID, rechecks each candidate in its transaction, records system history/audit entries, exposes health, and is deliberately unconfigured until publication.)
- [x] Upgrade compatible critical/high production dependencies, document residual advisories, and preserve application behavior through full validation. (Compatible AWS, Axios, Streamdown, and tRPC upgrades removed the critical advisory; five residual high advisories remain upstream review items under Express, Streamdown/Mermaid, Recharts, Drizzle, and NanoID.)
- [x] Add focused buyer/seller/admin regression coverage for P2 workflow contracts, verify responsive interfaces, and checkpoint only validated changes without publishing the website. (TypeScript and 33 Vitest files / 103 tests pass; desktop and mobile administrator verification, reservation-expiry, and notification settings interfaces were reviewed.)
- [x] Audit and document the current registration, server-enforced administrative hierarchy, admin-route access, seller publishing, listing lifecycle, evidence handling, moderation queues, and audit contracts before changing the control plane. (Confirmed public registration excludes administrative roles; existing server hierarchy protects seller/moderator/admin procedures; `/admin/*` is isolated; listings currently use manual video-evidence approval; listing and role enums now have safe additive lifecycle and support-role capacity.)
- [x] Introduce only the database and server lifecycle changes necessary for server-validated verified-seller auto-publishing, exception flagging, pause/out-of-stock/suspend moderation states, and retained auditability without weakening ownership or inventory checks. (Applied migrations 0014 and 0015; added PENDING_VALIDATION, FLAGGED, PAUSED, OUT_OF_STOCK, and SUSPENDED lifecycle capacity; persisted trusted image metadata; retained ownership, active-store, active-category, stock, audit, and atomic transition guards.)
- [x] Organize a dedicated role-aware `/admin/*` workspace that exposes only permitted sections to SUPER_ADMIN, ADMIN, MODERATOR, and operational support roles while preserving existing buyer/seller navigation isolation. (Administrator navigation now covers command, analytics, users, sellers, stores, moderation, verifications, categories, orders, reservations, offers, reports, disputes, reviews, notifications, audit, recovery, and settings; `/admin/operations` is summary-only for SUPPORT/MODERATOR/ADMIN/SUPER_ADMIN; `/admin/staff` is SUPER_ADMIN-only.)
- [x] Replace seller-facing per-product manual approval language with formal Publish product, live, validation, and review-exception states while retaining protected upload validation and truthful availability. (Seller catalogue and optional trust-video pages now describe Publish product, live success, flagged review exceptions, Pause, Archive, and Resume & publish; legacy evidence review remains supported only for existing PENDING_REVIEW records.)
- [x] Add controlled account/procedure tests for buyer, seller, moderator, admin, and super-admin boundaries; publishing validation; moderation/suspend/restore; audit records; dual-role shopping; and regression safety. (34 Vitest files / 111 tests pass, including direct-publication assessment, flagged exception behavior, public active-only policy, operational-support and super-admin boundaries, and protected flagged-listing restore/audit side effects.)
- [x] Validate desktop/mobile cross-role workflows, reconcile the checklist, checkpoint only completed work, and provide the requested admin-architecture, publishing, moderation, security, test, risk, and file-change handoff. (TypeScript passes; the seller catalogue, product moderation queue, administrator command center, role-scoped operations dashboard, and super-admin staff provisioning page were reviewed at 1280px and 375px. See `ADMIN_CONTROL_PLANE_AND_PUBLISHING_HANDOFF_2026-08-19.md`.)

- [x] Confirm the published deployment and pre-existing authenticated scheduler safeguards before enabling production reminder and reservation-expiry jobs. (Published `esutshop-59wzg8bs.manus.space` returned HTTP 200; both callback routes are registered; each callback checks cron identity plus its persisted task UID.)
- [x] Enable the published protected product-reminder and reservation-expiry schedules without changing their cron intervals or callback authentication contracts. (Enabled project-owned `product-reminders` at `0 */10 * * * *` and `reservation-expiry` at `0 */5 * * * *`; durable settings and super-admin audit entries store their task UIDs.)
- [x] Verify registered job state, recent execution health, idempotent empty-run safety, and administrator health projections; then checkpoint the activation outcome. (Both jobs report enabled. Product reminder delivery returned HTTP 200 for zero-work and genuine due-reminder runs; the latter processed one reminder and created one in-app alert. Reservation expiry returned HTTP 200, expired two real overdue reservations, and persisted zero-failure health.)

- [x] Trace and repair the seller store-application phone validation failure at review stage 2, preserving normalized Nigerian phone-number requirements and actionable field-level feedback. (All relevant server contracts now accept correctly formatted Nigerian mobile inputs, normalize valid values to E.164 storage, and reject invalid numbers. Seller onboarding presents `tel` input guidance and blocks malformed entries before submission.)
- [x] Replace raw Zod validation payloads in password-login errors with clear, non-sensitive inline guidance while preserving strict email and password validation, generic credential failures, and rate-limit protections. (Shared tRPC formatting now suppresses raw schema payloads. The login form performs clear email/password checks before mutation, keeps generic server credential errors and throttling intact, and accepts pre-existing password lengths at login while retaining a 10-character registration minimum.)
- [x] Add focused regression coverage, validate the repaired seller-application and login flows, reconcile the checklist, and save a checkpoint. (TypeScript passes; 34 Vitest files / 112 tests pass. Login, registration, and seller routes were reviewed at desktop layout; focused tests cover formatted Nigerian phone normalization, canonical persisted values, and invalid-number rejection.)

- [x] Audit existing account-level failed-password and IP-level authentication throttle lockouts, keeping their current anti-brute-force behavior server-authoritative. (Confirmed the existing five failed-password attempts / 15-minute account lock and IP-scoped authentication throttle; neither is replaced by client state.)
- [x] Return a safe retry timestamp for active account or request-level lockouts without revealing whether an email belongs to a marketplace account. (Only existing `TOO_MANY_REQUESTS` lockout responses carry a validated ISO retry timestamp; invalid credentials remain generic and do not disclose account existence.)
- [x] Add an accessible login countdown that disables submission until the server-authorized retry time, then automatically restores the form. (The login page derives its countdown from the retry timestamp, announces the wait time through an aria-live status, disables the email field and submit action while locked, and restores the form at zero.)
- [x] Add focused lockout/countdown regressions, validate the user experience, reconcile the checklist, and save a checkpoint. (TypeScript passes; 35 Vitest files / 115 tests pass, including threshold-lockout retry metadata and client countdown tests. Authentication routes render cleanly after the repair.)

- [x] Inventory every persisted phone/email field and all client/server validation contracts; define one strict production policy with explicit compatibility treatment for existing records. (Reviewed `users.email`, `profiles.phone`, `sellerApplications.phone`, seller-application attempt phones, store contact phones, and seller-verification emails. New submissions use shared normalized strict email and canonical Nigerian mobile validation; historical records are not destructively rewritten.)
- [x] Perform a security audit of every authentication, password recovery, password change, seller verification, and store-onboarding endpoint; document verified safeguards and any remediated gaps. (Completed `REDIS_SECURITY_PUBLIC_DEPLOYMENT_AUDIT_2026-08-20.md`, covering access boundaries, non-disclosure responses, token storage, onboarding ownership/verification gates, and explicit operational constraints.)
- [x] Add a Redis-backed authentication lockout store using a server-only `REDIS_URL`, preserve safe retry timestamps and HTTP 429 behavior, and define an explicit secure fallback when Redis is unavailable. (Configured secret passed server-only TLS PING. A pooled RESP client stores HMAC-derived temporary counters/lockouts, uses atomic Lua decisions, returns server-issued retry timestamps, and falls back to existing durable database throttles when Redis is unavailable.)
- [x] Add automated coverage for strict contact validation, Redis lockout semantics, retry metadata, endpoint authorization, and non-disclosure guarantees; validate, checkpoint, and deliver a production-readiness report. (37 regular test files / 119 passed + 1 intentional live Redis write test skipped by default; the opt-in Redis suite and secret connectivity suite pass with 4/4 tests.)

- [x] Enforce namespaced, HMAC-derived Redis keys with atomic counters, explicit TTLs, retry timestamps, and no permanent marketplace or raw PII data in Redis. (The shared security adapter uses `esut-marketplace:v1:security` HMAC-derived keys and only short-lived counts/lock keys; tests prove raw identifiers are absent from keys.)
- [x] Extend shared abuse protection to password reset, account registration, email-verification requests, seller application submission, and message sending using endpoint, IP-derived, and account-derived limits as appropriate. (Public authentication endpoints have shared endpoint/IP-derived limits; password reset adds an HMAC email limit with generic output; seller verification/application and messaging have account-scoped controls. A separate email-verification resend endpoint does not currently exist, so no unprotected resend path is introduced.)
- [x] Implement conservative Redis-outage behavior that neither exposes infrastructure details nor silently bypasses critical authentication protections; add durable, privacy-minimized audit observability for lockouts and abuse violations. (Redis failures return no infrastructure detail to users, activate database fallback, issue a rate-limited server warning, and record meaningful account lockout or protected-procedure abuse events without raw PII.)

- [x] Complete a controlled baseline audit of sessions, security events, Redis health, review/rating data, order identity, case evidence, profile/store media, notifications, and privileged access before extending working systems. (Completed before the approved additive production-trust milestones; working systems were extended rather than replaced.)
- [x] Add server-enforced per-device sessions, account-security events, current-device visibility, server-side session revocation, and recent reauthentication protection for critical root-admin actions. (Completed with server-enforced opaque session claims, active-session verification and revocation, privacy-minimized security events, the protected Security & devices workspace, and recent reauthentication enforcement for critical root-admin actions.)
- [x] Add a root-admin Redis security-health widget and deduplicated, privacy-safe account-lockout email-alert configuration using the existing notification provider. (Completed with the protected `/admin/security` workspace and audited configuration procedure.)
- [x] Redesign verified-purchase review and seller-rating experiences, add secure avatar and optional review-media workflows, and preserve truthful rating aggregation. (Completed with truthful public summary/distribution UI, completed-order eligibility, display-safe buyer context, opt-in public avatars, optional bounded image uploads, and published-review-only media projection.)
- [x] Verify and consistently present immutable server-issued order references; replace text-only report/dispute prompts with access-controlled case evidence, immutable activity history, and structured forms. (Completed with structured buyer dispute/pickup forms, message/store/review report dialogs, case evidence submission/retrieval controls, and immutable audit/activity records.)
- [x] Add allowlisted seller storefront controls and durable notification-event updates that refresh active buyer, seller, and administrator workspaces without full-page reloads. (Completed with plain-text allowlisted storefront fields, server-validated featured listings, public rendering, a caller-scoped latest-event query, and role-aware 30-second invalidation.)
- [x] Add authorization, IDOR, evidence-access, session, lockout-alert, order-reference, upload, notification, and cross-role regression coverage; validate all affected flows and produce the approved completion report. (Completed with focused production-trust regressions, full test/type validation, and `PRODUCTION_TRUST_COMPLETION_REPORT_2026-08-20.md`.)
- [x] Write and deliver a comprehensive ESUT Marketplace website report covering product operations, architecture, implemented features, security, testing, deployment, operating boundaries, and recommendations. (Completed in `ESUT_MARKETPLACE_FULL_WEBSITE_REPORT_2026-08-20.md`.)
- [x] Diagnose and fix unreliable public image and ESUT logo rendering, including broken asset delivery and resilient visual fallbacks. (Replaced the 6.1 MB header/footer PNG with a 24 KB managed WebP logo, added an inline branded fallback, added labelled listing-photo failure/absence states, and validated the fix.)
- [x] Add automatic high-quality seller product-photo optimization that preserves aspect ratio and product detail while reducing delivery weight. (Implemented browser-side high-quality WebP optimization for oversized seller images, with 1920 px maximum edge, no cropping, 0.92 quality, preservation of already-efficient images, existing server validation, and focused regression coverage.)
- [x] Deploy ESUT Marketplace through a staged, reversible Vercel migration using a temporary vercel.app URL, without custom-domain cutover until post-deployment validation is complete. (Explicitly paused by the project owner; not executed and remains outside this release.)
- [x] Conduct a focused pre-deployment audit of broken links, missing assets, UI/UX defects, runtime health, and performance bottlenecks; deliver a prioritized repair register before making fixes. (Completed in `PRE_DEPLOYMENT_AUDIT_2026-08-20.md`; confirmed repair work remains explicitly pending below.)
- [x] Resolve verified ESUT sending-domain readiness and validate ordinary-recipient account, recovery, order, and security email delivery before external hosting cutover. (Explicitly deferred by the project owner; not executed because no authorized ESUT sender domain/DNS access is available.)
- [x] Split large route bundles and defer heavy buyer, seller, moderator, and administrator modules to reduce the 1.70 MB initial JavaScript asset. (Completed with route-level lazy loading and deterministic vendor chunks; the entry application asset is now 118.86 KB / 17.78 KB gzip, with shared runtime assets split below the 500 KB warning threshold.)
- [x] Replace remaining legacy 6.1 MB logo references in account-entry and password-recovery pages with the optimized managed ESUT Marketplace asset. (Completed with shared brand asset constants across storefront, login, registration, and recovery pages; focused regression and TypeScript checks passed.)
- [x] Require a real primary product photo for new listing publication, guide existing sellers to add images, and replace repeated generic missing-photo cards with category-aware honest fallbacks. (Publication already enforces a real image; seller guidance now explains the gate and public cards render category-aware, explicitly labelled fallbacks from server-projected category metadata.)
- [x] Add structured category-aware product attributes and improve product-detail specification scanability without replacing seller-authored descriptions. (Recognised labelled details now render as accessible specification cards while unrecognised seller text remains intact.)
- [x] Replace remaining administrator browser-prompt note flows with accessible structured audit-note dialogs. (Completed for user access, store, listing/evidence, report, dispute, and review actions; source regression confirms no browser-native prompts remain.)
- [x] Strengthen mobile marketplace identity and distinguish discovery shelves with restrained editorial visual treatment. (Added a persistent compact mobile ESUT Marketplace wordmark and purpose-specific shelf treatments for deals, trending, campus essentials, and services; mobile rendering was visually verified.)
- [x] Add pre-cutover production monitoring for runtime errors, core web vitals, upload failure rate, asset failures, and security-relevant operational alerts. (Implemented an additive privacy-minimized operational-events ledger, Redis-bounded anonymous ingestion, client/runtime/API/asset/upload/core-vital signals, and restricted administrator aggregate health; existing immutable security-alert monitoring remains active.)

- [x] Additive milestone: created and applied migrations 0016/0017 for server-tracked sessions, privacy-minimized account security events, validated avatar/storefront metadata, private evidence/history, review media, and durable event records without modifying existing marketplace rows.
- [x] Additive milestone: issued tracked opaque-session JWT claims, enforce active server-side session records, provide current/other-device security visibility and revocation endpoints, and verify privacy-safe device/IP fingerprint behavior.
- [x] Additive milestone: added root-only safe Redis health projection plus controlled lockout-alert settings using existing provider infrastructure, with no credentials, keys, hostnames, raw IPs, tokens, or error details exposed in UI/API projections.
- [x] Additive milestone: implemented real authenticated buyer avatar upload/replacement/removal/privacy procedures and profile-photo workspace with MIME, byte-signature, size, storage-key, ownership, and audit controls.
- [x] Execute the owner-approved staged migration from MySQL/TiDB-compatible storage to organization-owned managed PostgreSQL, beginning with read-only compatibility inventory and preserving the existing database as the source of truth until reconciliation, recovery, and cutover gates are approved. (Explicitly deferred; no migration or production configuration change was made.)
- [x] Deliver a provider-neutral Cloudflare-versus-Hostinger architecture comparison against the existing ESUT Marketplace stack, identifying safe service roles, runtime constraints, external dependencies, operating responsibilities, and a no-change controlled-launch recommendation. (Completed in `CLOUDFLARE_HOSTINGER_ARCHITECTURE_COMPARISON_2026-08-21.md`.)
- [x] Design a safe Cloudflare cache and R2 integration blueprint that reduces public-read and media delivery pressure on MySQL while preserving MySQL as the sole authoritative commerce database. (Completed in `CLOUDFLARE_CACHE_R2_INTEGRATION_BLUEPRINT_2026-08-21.md`; no implementation, provider configuration, storage migration, DNS change, or cache activation is authorized.)
- [x] Document a secure Cloudflare Pages deployment pipeline and frontend-safe environment-variable model for the existing React/Vite application. (Completed in `CLOUDFLARE_PAGES_PIPELINE_ENVIRONMENT_GUIDE_2026-08-21.md`; guidance only, with no Cloudflare project, DNS, secret, source code, or deployment configuration change.)
- [x] Document a production-safe malware-scanning, file-promotion, database-backup, object-storage-backup, and restoration blueprint for the current Node.js, MySQL, and managed-storage architecture. (Completed in `MALWARE_SCANNING_AND_RECOVERY_BLUEPRINT_2026-08-21.md`; guidance only, with no scan service, bucket policy, scheduler, backup, restore, database, storage, or provider configuration change.)
- [x] Draft the owner-controlled phased infrastructure action plan for Cloudflare frontend delivery, the current/future Node backend, storage zones, scanning, backups, and cutover. (Completed in `ESUT_MARKETPLACE_INFRASTRUCTURE_ACTION_PLAN_2026-08-21.md`; planning only.)
- [x] Execute the owner-approved staged infrastructure action plan: governance and recovery targets; domain/DNS and Cloudflare governance; protected Pages staging; secure frontend-to-Node API connectivity; future Node-host readiness; caching/media; storage zones/scanning; backup/restore operations; and controlled cutover. Each phase requires its own explicit `READY` approval and tested rollback gate. (Deferred; no infrastructure action was authorized in this release.)
- [x] Phase 1: record organization ownership, recovery contacts, operating roles, initial service budget, data-retention policy, and approved RPO/RTO targets before any provider or production configuration work begins. (Deferred pending explicit governance authorization; no production/provider changes made.)
- [x] Phase 1: inspect existing connected apps and map their actual service access to the marketplace architecture before requesting any duplicate provider setup or ownership decision. (Confirmed enabled Cloudflare, Cloudflare Worker Bindings, Cloudinary Asset, GitHub, Resend, and Vercel integrations; no Hostinger connector is currently configured.)
- [x] Phase 1: refresh the connector inventory after the owner’s newly connected apps and map any changed service access to the marketplace architecture before taking provider actions. (Confirmed enabled Cloudflare, Cloudflare API, Cloudflare Worker Bindings, Cloudinary Asset, GitHub, My Browser, Resend, and Vercel integrations; no Hostinger connector is currently configured.)
- [x] Phase 2: create and validate the protected `esut-marketplace-staging` Cloudflare Pages project from the existing GitHub repository using only a temporary `*.pages.dev` address; preserve the current managed live site and do not connect a custom domain, API route, database, storage, or email service in this phase. (Created through the account-scoped Pages API at `esut-marketplace-staging.pages.dev` with no custom domain or production deployment; later staging API-boundary work is tracked separately.)
- [x] Phase 2a: authorize Cloudflare Pages Git integration for the private `Viper-Empire/esut-newmarketplace` repository before creating the staging project; grant repository access only and do not modify production source, domains, or deployment settings. (The owner authorized the repository in Cloudflare; the staging uploads remain branch-preview-only.)
- [x] Phase 2b: analyze the Git-integrated Cloudflare Worker Builds form and decide, before deployment, whether a static-assets Worker is an approved replacement for the planned Pages staging path; document field values, source-change implications, and least-privilege token requirements. (Rejected the Worker form for this phase because it would introduce an unnecessary Worker runtime and broader automatic token/source-change implications; the Pages-only route was documented and used.)
- [x] Phase 2c: choose the lowest-risk Cloudflare staging route on the owner’s behalf after verifying the newly enabled Worker Bindings integration; do not deploy the current Worker form unless the required static-assets, SPA-routing, same-origin API, and least-privilege controls are ready. (Decision: retain the current managed live baseline and do not deploy the unconfigured Worker form; Worker Bindings remains reserved for a later, reviewed proxy/storage phase.)
- [x] Phase 3: create an isolated `esut-marketplace-staging` Cloudflare Pages project through the validated Pages API, then prepare the Pages frontend build and a staged same-origin API boundary without changing the existing live marketplace, DNS, database, Redis, storage, or email services. (Completed at `https://staging.esut-marketplace-staging.pages.dev`; public homepage, product, login, and explore routes were verified. No live-service configuration, DNS, custom domain, database, Redis, storage, or email setting changed.)
- [x] Phase 3a: add and test a Cloudflare Pages Function proxy for staging-only `/api/*` requests so the deployed React frontend preserves the current same-origin tRPC and OAuth paths while the managed Node backend remains authoritative. (Added the allowlisted `/api/*` Function proxy; browser verification confirmed a same-origin anonymous tRPC response with JSON, private no-store caching, and noindex headers; focused proxy tests pass.)
- [x] Phase 3b: repair the production Vite chunk cycle exposed by the Cloudflare Pages preview so the React frontend mounts correctly without regressing route-level lazy loading or the existing bundle-performance work. (Replaced the cyclic catch-all vendor split with explicit one-way React/router, data, and interface runtime groups; final staging deployment `6e71386e` mounted and loaded real public collections; 47 Vitest files / 140 tests passed, with independent lazy page modules preserved.)
- [x] Remove the Light/System/Dark mood preference system, its persistent browser state, and all buyer, seller, administrator, and settings controls while preserving the original ESUT visual appearance. (Removed the custom theme context, selector component, account/settings control, workspace-header control, dark-mode CSS, root mutations, and unused `next-themes` dependency; legacy preference state is cleared once on application load.)
- [x] Add focused regression coverage and desktop/mobile visual validation proving the marketplace remains consistently accessible in its original appearance after mood-system removal. (Added `moodSystemRemoval.test.ts`; type check, production build, and 47 Vitest files / 139 tests passed with 1 intentional skip. Desktop and 375px public/account-boundary captures retain the original ESUT appearance and show no mood controls.)
- [x] Add accessible animated loading skeletons for marketplace listing collections while server data is being fetched, preserving existing error, empty, and real-data states. (Added reusable motion-reduced product-grid skeletons plus category/store shelf placeholders; Home and Explore preserve their existing server-error, empty, and real-listing branches.)
- [x] Add a protected seller listing-management dashboard for authenticated verified sellers to view, edit, manage stock, and control permitted listing lifecycle actions using only their own real listings. (Enhanced the existing `seller.products` workspace into a filterable **My listings** manager with live/draft/attention/archive counts and safe links/actions; server-side verified-seller and owner checks remain authoritative.)
- [x] Validate the loading and seller-management improvements across authorization, responsive UI, tests, and the Cloudflare staging preview; redeploy the current fixed-appearance build so the preview no longer shows legacy mood controls. (Focused tests, full test suite, TypeScript, and build passed; desktop rendering and protected seller gate were visually checked. Deployment `bdd96c67` was directly verified on Home and Explore, and its rendered DOM contains neither `Theme preference` nor `Light System Dark`.)
- [x] Add direct, server-authorized seller listing edit and delete controls, with deliberate confirmation, ownership enforcement, and truthful lifecycle handling. (Added guarded seller draft deletion policy and procedure with owner/verified-seller enforcement, deliberate confirmation UI, focused regression coverage, and no automatic product-media deletion.)
- [x] Add a polished empty state with an illustration and Create listing action when a verified seller has no listings. (Added the real no-listings state to the protected My listings workspace with an illustrated icon treatment and direct Create listing route.)
- [x] Add marketplace discovery search and category filtering controls that work with the existing real listing search contract and preserve current filter/sort behavior. (Added visible category controls to the existing public discovery search contract; filtering, sorting, empty, and loading paths retain real listing data.)
- [x] Redesign buyer and seller authenticated messaging as a polished social-style conversation UI with authorized live updates and typing-state feedback; do not fabricate conversations or message data. (Replaced basic message presentation with a dedicated authorised two-pane marketplace chat workspace, safe real conversation search, product/store context, responsive thread layout, and no fabricated conversations or presence.)
- [x] Implement the approved secure fast-refresh chat model: active-thread updates and participant-authorized typing signals with automatic expiry, without an always-on socket runtime. (Added additive expiring typing-state storage, participant-only procedures, rate limits, focused active-thread/list refresh, and durable recipient refresh signals; full test suite passed locally.)
- [x] Apply the supplied dark, high-contrast social-style reference to buyer and seller messaging: selected conversation treatment, compact listing context, rounded received/sent bubbles, accessible composer, and responsive one-column mobile flow. (Applied a marketplace-specific version of the reference with only safe supported actions, current listing/store context, report action, reduced-motion treatment, and responsive mobile back navigation.)
- [x] Make the new chat palette respond only to the device `prefers-color-scheme` setting, without restoring a user theme selector, persistent mood preference, or global Light/System/Dark controls. (Scoped system-adaptive colour tokens to the chat workspace; no global theme context, selector, or persistent appearance setting was restored.)
- [x] Analyse the supplied `esut_chat_system.html` reference and translate its appropriate buyer–seller chat layout, interaction, accessibility, and marketplace-context patterns into an implementation specification before changing the messaging interface. (Completed in `CHAT_REFERENCE_DESIGN_ANALYSIS_2026-08-22.md`; separated adoptable real-data patterns from unsupported/fabricated reference controls.)
- [x] Validate the seller, marketplace, and messaging changes through focused/full tests, responsive UI checks, authorization checks, and a refreshed Cloudflare staging preview. (Current Cloudflare Pages staging deployment `2817b465` served the public app and stable `staging` alias; full Vitest, TypeScript, production build, direct proxy probes, browser console/resource checks, protected-route checks, and Playwright smoke suite passed.)
- [x] Diagnose and repair the repeated staging `/api/trpc/observability.record` unexpected-response failure, including retry containment and same-origin Pages proxy validation. (Expanded default/branch Pages-host detection, added single-flight and five-minute telemetry circuit-breaker containment, preserved telemetry-recursion suppression, and documented the incident in `STAGING_OBSERVABILITY_REPAIR_2026-08-23.md`; focused telemetry/staging/proxy tests and TypeScript passed. A live Pages refresh remains blocked by the owner’s Cloudflare pause.)
- [x] Produce an owner-controlled Cloudflare Pages request-budget strategy covering public-read caching, authenticated no-store boundaries, polling limits, telemetry safeguards, and staging-only traffic controls after the daily Functions limit was exceeded. (Completed in `CLOUDFLARE_PAGES_REQUEST_BUDGET_AND_CACHING_PLAN_2026-08-22.md`; no caching or polling runtime setting was changed.)
- [x] Compare current Vercel and Cloudflare request/function limit models for ESUT Marketplace staging, including frontend-only versus proxied API traffic, and recommend a low-risk path without changing either provider configuration. (Completed from current official provider documentation; Vercel has usage limits and Hobby is non-commercial only, so no provider move was approved.)
- [x] Pause all Cloudflare staging, API-proxy, caching, and provider-migration work by owner direction; use the managed Manus public website as the sole public environment until new authorization is given. (Public baseline confirmed available at `https://esutshop-59wzg8bs.manus.space`; no Cloudflare or Vercel setting was changed.)
- [x] Validate and release the current seller listing, discovery, messaging, and telemetry-containment enhancements through the managed Manus website without relying on Cloudflare Pages Function availability. (Owner published checkpoint `1e2ad855`; public home and discovery filters, signed-out message boundary, authenticated private conversation workspace, verified seller My listings workspace, and direct edit form were read-only verified on `https://esutshop-59wzg8bs.manus.space`. No real listing or message was modified.)
- [x] Audit the supplied `Design systems for ESUT Marketplace.zip` and `pasted_content_2.txt` against the existing real ESUT Marketplace authentication, session, role, seller-onboarding, and Redis security flows before changing login or registration UI. (Completed in `AUTH_DESIGN_SYSTEM_AUDIT_2026-08-22.md`; the ZIP was inspected as untrusted design data only and no source was executed, installed, or imported.)
- [x] Produce a production-safe ESUT-green authentication redesign specification that adopts only the supplied visual language, preserves real backend actions and dual-role account behavior, and identifies any explicitly deferred email/verification capability. (Completed in `AUTHENTICATION_REDESIGN_IMPLEMENTATION_PLAN_2026-08-22.md`; implementation remains pending explicit approval.)
- [x] Refactor the login and registration presentation into the approved ESUT-green responsive authentication design while preserving current real `auth.login`/`auth.register` mutations, lockout countdown, native registration-intent semantics, and server-confirmed redirects. (Implemented shared ESUT-green auth components, real mutation wiring, server-derived lockout countdown, native radio-card intents, session refresh, and server-confirmed redirects; no social or mock authentication was added.)
- [x] Refactor forgot-password, reset-password, and verification presentation into the shared authentication design while preserving real availability/token contracts and truthful paused-email boundaries. (Implemented truthful recovery availability/failure states, token-bound reset/verification views, server-confirmed reset success with session refresh, and no unsupported email or resend claims.)
- [x] Add focused regression coverage for the redesigned auth component contracts and run full authentication/security tests, responsive visual checks, and owner-controlled managed-site verification before release. (Added `AuthRedesign.contract.test.ts`, aligned browser smoke coverage with the native radio controls, passed TypeScript, 54 Vitest files / 155 tests with 1 intentional skip, production build, and 20/20 browser scenarios; desktop/mobile captures and a read-only managed recovery-route verification passed after publication propagation.)
- [x] Produce the approved Authentication Redesign Report covering adopted components, preserved backend/security controls, dual-role/seller integration, responsive/accessibility work, tests, and remaining delivery limitations. (Delivered as `AUTHENTICATION_REDESIGN_REPORT_2026-08-22.md`; it records the remaining paused sender-domain boundary and successful read-only managed-site verification.)

- [x] Diagnose the reported public product-card visual defects—including missing seller-photo placeholders, inconsistent image treatment, and offer-price presentation—against the supplied screenshots and current implementation before any corrective change. (Completed in `PRODUCT_CARD_VISUAL_DIAGNOSIS_2026-08-22.md`: confirmed six active listings without images, two persisted primary-image paths returning live 404s, valid current discount data, and no card-overlap or fake-content defect. No listing, media, or visibility data was changed.)

- [x] Implement a SUPER_ADMIN-controlled password replacement workflow that never reveals existing passwords, requires a target user and audit reason, revokes other active sessions, and records a privacy-safe immutable audit event. (Implemented `admin.resetUserPassword` behind `superAdminProcedure` with fresh acting-admin password confirmation, new-password confirmation, scrypt hashing, target lockout reset, target session revocation, reset-token invalidation, Redis lockout clear attempt, target security event, and privacy-safe immutable audit entry. Added a SUPER_ADMIN-only recovery dialog plus executable/contract/authorization regressions; TypeScript, 56 Vitest files / 161 tests with 1 intentional skip, production build, and unauthenticated-route visibility check passed.)

- [x] Bind an owner-approved custom domain to the existing Manus-hosted ESUT Marketplace without changing its backend/data architecture, retain the current `manus.space` URL as a rollback path during validation, and document the verified production-domain outcome. (Explicitly deferred; current `manus.space` URL remains the active public address.)

- [x] Plan the production storage and media architecture using Manus-managed object storage, with public/private/temporary zones, secure upload validation, access controls, lifecycle, recovery, and future scaling boundaries. (Delivered in `STORAGE_MEDIA_ARCHITECTURE_PLAN_2026-08-22.md`; no storage objects, schema, provider, permissions, or deployment settings were changed.)

- [x] Run the approved read-only media-integrity audit across active listings and media classes without uploading, deleting, migrating, or changing visibility. (Completed in `MEDIA_INTEGRITY_AUDIT_2026-08-22.md`: 8 active listings, 6 with no image rows, 3 active image rows across 2 listings, both checked primary objects returning managed-route Not found, 1 listing video, 4 verification-document references, and no review/case/avatar media rows. No data or visibility changed.)

- [x] Assess the enabled Cloudinary Asset integration and define a safe role for image transformation/delivery or future media migration without weakening Manus storage, private evidence access, or server-side ownership controls. (Completed in `CLOUDINARY_INTEGRATION_ASSESSMENT_2026-08-22.md`; Cloudinary is approved for public product images, derivatives, explicitly public avatars, and ESUT public branding, while Manus remains authoritative for private/protected media.)

- [x] Integrate Cloudinary as the public media layer for approved product images, optimized card derivatives, explicitly public avatars, and ESUT public branding, while retaining Manus storage for private evidence and protected media with a tested fallback path. (Implemented server-signed Cloudinary image uploads for new product images and explicitly public avatars, optimized allowlisted delivery URLs, Manus fallback, verified Cloudinary public ESUT logo, server-only credentials, and regressions. Existing media was not migrated or altered.)

- [x] Keep `https://esutshop-59wzg8bs.manus.space` as the official public ESUT Marketplace URL; defer custom-domain purchase, connection, redirect, disabling, and DNS changes until the owner explicitly requests them. (Recorded in `HOSTING_DOMAIN_DECISION_2026-08-22.md`; no domain, DNS, routing, or runtime setting changed.)

- [x] Analyze and, if approved, improve product-card alignment and vertical rhythm so titles, prices, seller metadata, locations, and Add to cart actions form a consistent responsive grid without altering truthful listing data. (Added stable two-line title slots, price-row height, bounded seller/location rows, full-height flex cards, and bottom-aligned actions. Desktop and 375px visual checks passed; focused storefront tests, TypeScript, production build, and full suite passed: 58 files, 165 tests, 1 intentional skip.)

- [x] Restyle the authenticated buyer, seller, and administrator dashboard workspaces to match the approved ESUT-green login system, with accessible contrast/focus states and no change to dashboard permissions, data, navigation, or removed mood controls. (Updated the shared buyer/seller workspace shell plus administrator overview/subpage shell: ESUT-green sidebar/menu, ESUT-red active navigation/actions, gold status accents, white/green surfaces, scoped focus styling, and mobile-safe navigation. Added `DashboardTheme.contract.test.ts`; full suite passed with 59 files, 168 tests, and 1 intentional skip; TypeScript and production build passed. Signed-out desktop/mobile route checks confirmed protected boundaries; authenticated content requires an owner login session for final visual confirmation.)

- [x] Diagnose and fix the reported cross-browser product-photo failure on product-detail pages, preserving authentic media, Cloudinary public delivery, Manus private storage, ownership checks, and truthful fallback states. (Root cause and same-origin streaming remediation are documented in `EDGE_PRODUCT_MEDIA_REPRODUCTION_NOTES_2026-08-22.md`.)

- [x] Fix public product-image delivery for browser/Edge requests when the managed storage proxy returns divergent results from plain HTTP clients; preserve private evidence boundaries and add regression coverage. (Same-origin server-side streaming proxy implemented; browser decoded the affected 810×1080 JPEG on the dev route.)
- [x] Add an administrator read-only media-integrity report for active listings, including missing/unreachable public media and seller recovery guidance. (Administrator-gated report checks up to 100 active listings and never returns storage keys or signed URLs.)
- [x] Add a seller-authorized re-upload path for broken public listing images without exposing storage keys or permitting cross-seller mutation. (In-place replacement mutation and accessible seller-editor Replace controls added.)
- [x] Verify the buyer, seller, and administrator dashboard presentation on authenticated routes after the media remediation release. (Buyer and seller dashboards were reviewed in the prior authenticated session; administrator overview, moderation, analytics, settings, and audit-log surfaces were reviewed in the authenticated administrator session on 25/08/2026. Findings are recorded in `AUTHENTICATED_DASHBOARD_REVIEW_NOTES_2026-08-25.md`; no records or settings were changed.)
- [x] Save and publish a verified checkpoint for the media remediation release. (The approved Cloudinary migration and live browser verification are ready for checkpoint publication.)

- [x] Inspect why some products are absent or image-less on the public managed homepage/catalogue, cross-checking listing status, pagination, query filters, and media reachability without modifying data. (Verified all 8 ACTIVE listings appear in homepage and Explore; 6 lack primary-image rows and 2 have browser-facing managed-media 404s despite upstream 200 responses.)
- [x] Document the verified public product-visibility findings and safe remediation options. (Recorded in `PUBLIC_PRODUCT_VISIBILITY_AUDIT_2026-08-22.md`; no listing, media, account, or storage data was changed.)

- [x] Audit the current Cloudinary public-media integration and define a reversible migration boundary for authentic active listing images. (Existing signed upload helper and public/private boundaries reviewed; migration limited to two authentic active primary images.)
- [x] Migrate or re-home eligible public listing image references to Cloudinary without exposing private evidence or changing ownership/status data. (Two images migrated and verified; original Manus objects preserved; listing status, ownership, and primary flags unchanged.)
- [x] Verify Cloudinary delivery in the live browser path and confirm protected Manus media remains private. (Live homepage decoded both Cloudinary assets at 810×1080 and 720×720; no private media was migrated.)
- [x] Document Cloudinary rollout, fallback, and rollback behavior before publishing the migration checkpoint. (Recorded in `PUBLIC_PRODUCT_VISIBILITY_AUDIT_2026-08-22.md`; original objects remain available for rollback.)

- [x] Migrate the two owner-approved authentic active listing primary images to Cloudinary, verify both public assets, and update only their public image references with an auditable rollback path. (Cloudinary HTTP 200 verification and live browser decode passed for both images.)

- [x] Recover the persistent public maintenance page at `https://esutshop-59wzg8bs.manus.space/`, verify the live root and public routes, and document the deployment cause without changing marketplace data. (Rollback propagation completed; live root returned HTTP 200 with the ESUT Marketplace app and fresh browser verification passed. Incident recorded in `PUBLIC_DOMAIN_404_INCIDENT_2026-08-23.md`.)

- [x] Audit the current public marketplace query, filters, joins, pagination, media projection, and frontend response contract without changing production behavior. (Recorded in `MARKETPLACE_QUERY_AUDIT_2026-08-23.md`; current tRPC contract and live query semantics documented.)
- [x] Build and run read-only current-versus-proposed marketplace query comparison tests, record any mismatches, and keep the production query unchanged until review. (`server/marketplaceQueryComparison.test.ts` passed 2 tests across default, sorting, search, category, duplicate-detection, and media-coverage checks; TypeScript passed.)

- [x] Apply the compared optimized marketplace query behind the existing public tRPC endpoint contracts without changing visibility, ownership, status, or media rules. (Search now selects the paginated listing set first and resolves ordered primary public images separately; existing inputs, response envelope, visibility filters, and mapper are preserved.)
- [x] Add a truthful marketplace “No results found” empty state with accessible search suggestions and clear recovery actions. (Explore now shows a responsive SearchX state, real product/store/category suggestions, clear filters, and browse-all recovery; direct `?q=` URLs are synchronized correctly.)
- [x] Validate optimized endpoints, no-results behavior, responsive UI, and regressions before publishing. (Real-database comparison passed, full Vitest and TypeScript passed, production build passed, and the live dev no-results route was browser-verified.)

- [x] Add a visible, motion-reduced marketplace loading animation while search results are fetching. (Explore shows an accessible live status indicator during background fetches with reduced-motion support.)
- [x] Add a real authenticated “Notify me” action to the zero-results state using the existing reminder/notification backend, with a truthful signed-out boundary. (Dedicated `searchAlerts` persistence, ownership-protected create/list/cancel procedures, publication-time matching notifications, audit logging, and signed-out prompt added.)
- [x] Add explicit marketplace sorting labels including “Price: Low to High” and “Newest First,” preserving server-supported sort values. (Existing server sort values retained; Explore labels are explicit.)

- [x] Add a dedicated authenticated dashboard section for viewing active Notify me saved searches with truthful loading and empty states. (Added `/account/search-alerts` with active-count, loading skeletons, error retry, informative empty state, criteria, and last-match display.)
- [x] Add secure user-owned saved-search cancellation from the dashboard with confirmation, pending feedback, and cache refresh. (Server scopes cancellation to the caller’s active alert, records `SEARCH_ALERT_CANCELLED`, and the UI confirms, disables, toasts, and invalidates.)
- [x] Validate the saved-search dashboard route, ownership boundary, responsive UI, and deletion regressions before publishing. (Signed-out route shows no alert data; saved-search security tests, full suite, TypeScript, and production build passed.)

- [x] Add secure in-place editing for active saved-search criteria without recreating alerts or changing ownership. (Added server-owned `searchAlerts.update`, duplicate protection, audit event, and in-card edit form for query/category/price/condition/verified filters.)
- [x] Add persisted saved-search notification preferences for in-app and email delivery, with truthful delivery capability handling. (Added additive schema columns, validation requiring one channel, in-app gating, Resend-gated email dispatch, managed template support, and provider-aware copy.)
- [x] Validate saved-alert edit/settings persistence, authorization boundaries, responsive UI, and notification behavior before publishing. (61 test files, 175 passed, 1 skipped; TypeScript and production build passed; signed-out mobile route privacy verified.)

- [x] Audit public, buyer, seller, administrator, detail, form, and messaging routes for dead ends where users must use browser Back. (Mapped App routes and existing page-level navigation; prioritized cart, checkout, seller/admin detail, account detail, category, and auth recovery paths.)
- [x] Add contextual in-app return, marketplace search, dashboard, and parent-section links without weakening route authorization or form safety. (Added shared `ContextualNavigation` with parent, workspace, and Browse marketplace links; avoided redundant product/store/root-shell links.)
- [x] Validate navigation paths on desktop and mobile, including deep links, protected routes, and unsaved-form escape behavior. (Added route-contract tests; full suite, TypeScript, build, and six-route desktop visual review passed. Signed-out protected pages showed no private data.)

- [x] Push the owner-authorized current build to the isolated `esut-marketplace-staging` Cloudflare Pages project without changing the managed Manus public site, custom domains, DNS, database, Redis, storage, or email configuration. (Preview deployment `2817b465-8a3c-41c0-ad2a-0b1dc722b461` uploaded from build `76be291` to branch `staging`; managed Manus site and all listed production services were unchanged.)
- [x] Verify the Cloudflare staging preview across public marketplace routes, same-origin `/api/*` proxy behavior, protected boundaries, telemetry suppression, responsive rendering, and rollback safety; record the staging deployment result. (Verified immutable URL and stable alias, real homepage/catalogue/search/product data, Cloudinary media, anonymous account/seller/admin/messages boundaries, no deployed `observability.record` resource, no browser console errors, direct 200 JSON API responses, and 20 Playwright smoke scenarios. Results recorded in `STAGING_BROWSER_VERIFICATION_NOTES_2026-08-23.md`; bare project hostname remains intentionally branch-only 404.)

- [x] Verify the failed homepage visual edit target and apply the requested `Books` to `Beauty & Personal Care` label correction if it is absent, then validate and checkpoint the result. (The visual selector was stale; the hero label was updated manually and the live category route was corrected to use server names.)

- [x] Rename the real Books marketplace category to Beauty & Personal Care consistently across display labels, hero artwork, slug/route, category filters, backend references, and existing category-linked listing data without changing ownership or inventing listings. (Renamed category id 4 to `beauty-personal-care`, updated live filters/routes, hero artwork, icon mapping, saved-search criteria, and exact heading rendering.)

- [x] Create a real `Digital Books & Courses` category and move the existing Engineering Mathematics course-material listing to it without changing listing ownership, status, price, or media. (Created category id 30001, moved listing id 3 only by categoryId, and verified title, seller, price, status, inventory, and truthful image fallback.)
- [x] Rename the existing Books category record to `Beauty & Personal Care`, update its slug and description, and ensure all UI labels, hero artwork, category routes, filters, and tests use the new category names consistently. (Added `Home.categoryRename.test.ts` and `ExplorePage.categoryTitle.test.ts`; full Vitest, TypeScript, build, and visual route checks passed.)

- [x] Add a readable, accessible dark-mode toggle to the shared header across public and authenticated responsive layouts, with persistent preference, contrast-safe styling, reduced-motion support, and regression coverage. (Added controlled `DarkModeProvider`, persistent light/dark preference, reusable accessible toggle in storefront, buyer/seller workspace, admin suite, and standalone admin control pages, dark surface/text/form/chat overrides, theme-aware toasts, and updated contracts. Full Vitest, TypeScript, production build, desktop screenshots, and 375px mobile screenshots passed.)

- [x] Refine the dark-mode palette using the supplied burnt-orange, amber-brown, and olive-gold gradient direction while preserving readable contrast, light-mode defaults, and responsive control visibility. (Added warm cocoa surfaces, burnt-orange hero gradient, amber/olive accents, readable foreground tokens, responsive toggle styling, and a browser regression confirming toggle state, gradient application, accessible labels, and persistence after reload.)

- [x] Replace the warm-earth dark-mode colors with a cool, high-contrast charcoal, teal, ESUT-green, mint, and restrained gold combination while preserving the existing toggle, light default, and responsive readability. (Replaced the warm treatment with ink/slate backgrounds, teal and ESUT-green accents, mint foregrounds, restrained gold status emphasis, and a cool teal/green hero gradient. Full Vitest, TypeScript, production build, and browser toggle/persistence validation passed.)

- [x] Remove the dark-mode toggle, provider, dark palette overrides, and dark-mode browser regression while restoring the light-only ESUT Marketplace appearance and tests. (Removed provider, toggle integrations, explicit dark/system-dark overrides, and dark browser test; restored light toasts and runtime state; updated removal/dashboard contracts. Full Vitest, TypeScript, production build, and 20 serial Playwright smoke tests passed.)

- [x] Rename the real top-level categories `Hostel & Lodge` to `Accommodation` and `Food & Groceries` to `Food (ESUTChop)`, update canonical slugs/routes and all UI references, preserve linked data, and add compatibility redirects for old slugs. (Production rows id 5 and 6 were guarded and updated to `Accommodation`/`accommodation` and `Food`/`food`; linked listing counts were preserved, legacy saved-search references were migrated, and old hostel URLs normalize safely.)

- [x] Freeze and record the current category, listing, and saved-search mappings before the Food/Accommodation migration. (Recorded category ids 5 and 6, legacy slugs, observed active listing relationships, and preservation constraints in `VERTICAL_CATEGORY_BASELINE_2026-08-24.md`.)
- [x] Implement the approved Food → ESUTChop and Hostel & Lodge → Accommodation rename with canonical routes and old-link compatibility. (Updated server/client canonical presentation, dedicated `/esutchop` and `/accommodation` routes, old-slug redirects, API normalization, backend migration contracts, and UI tests; full regression, TypeScript, build, and route screenshots passed.)
- [x] Add safe parent/sub-category contracts and scaffold dedicated ESUTChop and Accommodation entry experiences using truthful empty states and existing real data only. (Added canonical category presentation helpers, `/esutchop` and `/accommodation` entry routes, safe legacy redirects, real Food query handling, truthful Accommodation empty state, retry feedback, responsive UI, and focused contracts.)

- [x] Verify the authorized Food/Accommodation production migration against live category, listing, saved-search, and foreign-key relationships before execution. (Confirmed ids 5 and 6, active status, sort order, preserved listing counts, zero legacy hostel saved-search references after migration, no stale legacy category rows, and no foreign-key reassignment.)
- [x] Apply and verify the non-destructive category-row rename with preserved IDs, linked data, canonical slugs, and legacy compatibility. (Updated only approved rows in one guarded transaction, preserved ids 5/6 and linked data, normalized old `hostel-home` lookups to `accommodation`, and passed focused/full tests, TypeScript, build, and route screenshots.)

- [x] Complete the owner-authorized read-only authenticated buyer, seller, and administrator dashboard review across desktop and mobile, including navigation escapes, loading/error states, light-only branding, and role boundaries. (Desktop/authenticated buyer, seller, and administrator review is complete; mobile-specific visual verification remains documented as a tooling-limited follow-up and was not falsely marked as independently verified.)

- [x] Investigate and recover the managed `manus.space` timeout without changing DNS, domains, database records, provider settings, or production data; verify public availability before resuming authenticated review. (Restarted the managed service; public root returned real marketplace content and `/account` returned the expected signed-out privacy boundary. No DNS, domain, database, provider, or production-data change was made.)

- [x] Audit every active listing against its real product title, description, specifications, category, and route to identify whether the Adjustable Study Lamp issue is isolated or part of a broader misclassification pattern. (Reviewed all eight active/out-of-stock/reserved real listings; the lamp was the only confirmed mismatch. Findings recorded in `CATEGORY_INTEGRITY_AUDIT_2026-08-24.md`.)
- [x] Correct all confirmed category mismatches with guarded category-only updates and verify non-category fields remain unchanged. (Moved listing id 4 from Accommodation to Electronics with ID/category/title guards; verified owner, price, condition, location, status, counters, timestamps, and media boundary were preserved. Full Vitest, TypeScript, build, and category-route checks passed.)

- [x] Fix the hero artwork clipping shown in the supplied image by preserving the full cart mark and Campus Deals composition across desktop, tablet, and mobile without changing the underlying artwork asset. (Centered the cart within a bounded responsive art panel, added a stable aspect-ratio/min-height floor, and adjusted mobile floating-card/caption insets. Verified at 1280px, 768px tablet, and the supplied 566px viewport; Vitest, TypeScript, and production build passed.)

- [x] Redesign the hero artwork into a more attractive, charming responsive composition with tasteful visual depth, smooth floating-card hover interactions, and a subtle first-load shopping-cart entrance animation while preserving ESUT branding, light-only appearance, safe clipping, keyboard accessibility, and reduced-motion behavior. (Added layered green/gold depth, a soft center glow, lightweight visual texture, hover lift/shine/focus states, and a short cart entrance/ring reveal. Added `Home.hero.test.ts`; focused tests, TypeScript, production build, desktop screenshot, and supplied mobile viewport verification passed. The unrelated live Resend credential probe timed out at its existing network boundary.)

- [x] Analyze `Userdashboard.zip` as a visual/design reference and redesign the ESUT Marketplace hero artwork into a quality, professional, unclipped responsive composition inspired by verified reference patterns without copying unrelated dashboard content. (Inspected the React/Vite reference, global styles, and both identical artwork assets; used the supplied Campus Deals artwork through managed static storage with `object-fit: contain`, a padded rounded frame, ESUT context badge, and restrained sheen. Added `USERDASHBOARD_REFERENCE_ANALYSIS_2026-08-24.md`; focused hero test, TypeScript, production build, and desktop/tablet/mobile visual checks passed. No unrelated dashboard content, fake data, or production records were copied.)

- [x] Rebuild the hero artwork to closely match the uploaded reference: dark ESUT-green rounded square, five orbiting category chips, centered ESUT Marketplace logo medallion, lower-left BUY · SELL · CONNECT pill, and lower-right Campus Deals lockup, with safe responsive containment and accessible motion. (Replaced the prior imported-artwork frame with a CSS/React reference-matched composition using the managed ESUT logo asset; added five labelled category chips, orbit rings, centered medallion, lower overlays, motion-safe focus states, and a tablet stacked breakpoint so CTA copy remains visible. Focused hero test, TypeScript, production build, and desktop/mobile/tablet visual checks passed.)

- [x] Add a subtle staggered floating animation to the five white hero category chips and a slow rotation effect to the orbit rings, using transform-only motion and disabling decorative animation for reduced-motion users without changing the reference composition or causing clipping. (Added a 6.8-second staggered chip float via CSS `translate`; added centered 42-second outer, 34-second reverse inner, and 54-second core orbit rotation; preserved hover/focus transforms and disabled both animations under `prefers-reduced-motion`. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Rearrange the five white hero category cards into a clear clockwise solar-orbit order, increase their visibility and spacing, and animate them around stable orbital paths without colliding with the medallion or Campus Deals lockup; preserve keyboard focus, reduced-motion behavior, and responsive containment. (Added a shared 16-second clockwise orbital path with ordered start angles for Fashion, Phones, Food & Snacks, Beauty & Care, and Books & Notes; strengthened white-card sizing/contrast, cleared legacy stretching offsets, reserved the Campus Deals safe zone, and supplied a static reference arrangement for reduced motion. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Make the five orbiting hero category cards clickable, keyboard-accessible links to their respective real marketplace category routes/sections, and add a subtle central ESUT Marketplace logo glow pulse that preserves readability and disables for reduced-motion users. (Linked Fashion, Phones, Food & Snacks, Books & Notes, and Beauty & Care to canonical routes; added accessible labels, preserved focus styling, and added a low-intensity gold halo behind the logo with reduced-motion fallback. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Replace the hero category chips with polished icon-led cards, add a sixth Accommodation card linking to `/accommodation`, and remove continuous orbital rotation while preserving stable orbit framing, clickable links, visible labels, responsive containment, focus states, and reduced-motion safety. (Added icon tiles and six balanced cards for Fashion, Phones, Food & Snacks, Accommodation, Beauty & Care, and Books & Notes; added the real `/accommodation` route; removed continuous ring/card rotation; corrected side insets so cards remain fully contained. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Redesign the hero category cards as compact rounded-square white tiles with centered colorful icons above centered labels, matching the supplied reference; preserve six real category links including Accommodation, balanced placement, static orbit framing, logo glow, responsive containment, focus states, and reduced-motion safety. (Reworked all six cards into vertical icon-over-label tiles; preserved real routes including `/accommodation`; removed rotation, balanced the lower Beauty & Care placement, and maintained static orbit rings, logo glow, hover/focus states, and reduced-motion fallback. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Add a smooth clockwise staggered entrance animation for the six hero category tiles, organize their positions in a deliberate readable order, and reintroduce restrained slow orbit-ring rotation behind the stable tiles without clipping or obscuring the logo/caption; preserve hover/focus and reduced-motion behavior. (Reordered DOM traversal to Fashion, Phones, Food & Snacks, Beauty & Care, Books & Notes, Accommodation; added 720ms first-load reveal with 80ms stagger; restored ring-only rotation at 56s/44s reverse/68s while keeping tiles fixed; reduced-motion disables both. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Readjust the six hero category tiles into a more spacious professional arrangement with clear gaps around the logo medallion and Campus Deals caption, while preserving the clockwise entrance order, slow ring rotation, category links, and responsive containment. (Moved upper cards to 14%/18–82%, side cards to 48%/10–90%, and lower cards to 72–75% with separated lanes; mobile uses dedicated safe insets. Preserved clockwise entrance timing, slow ring-only rotation, real links, and reduced-motion behavior. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Reposition the six hero category tiles to mirror the supplied reference with wider breathing room, keep Accommodation as a balanced sixth tile, and add calm orbit-frame rotation with restrained pop-in/soft-breathe motion without fast card travel, clipping, or caption overlap. (Matched the reference positions with upper left/right cards, side-middle cards, and separated lower lanes; added 780ms pop-in with 80ms clockwise stagger and retained slow ring-only rotation behind fixed cards. Preserved real links, responsive containment, and reduced-motion fallback. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Move Beauty & Personal Care to the right-middle position used by Food & Snacks, and move Food & Snacks to a distinct lower-right position without crowding the Campus Deals caption or clipping at desktop/mobile sizes. (Beauty & Care now occupies the right-middle reference position; Food & Snacks moved to the lower-center lane; desktop/mobile visual checks confirm clear separation and containment. Focused hero test, TypeScript, and production build passed.)

- [x] Implement true controlled orbital movement for the six hero category cards with equal angular spacing, upright readable labels, collision-safe radius, hover/focus pause, and reduced-motion static fallback while keeping the logo and Campus Deals lockup unobstructed. (Added structural orbit carriers with six equal phase positions at a 120px desktop radius, upright counter-rotation for the nested links, hover/focus pause, and a static reduced-motion layout. Fixed the prior collapse-to-center issue caused by conflicting direct-link transforms. Focused hero test, TypeScript, production build, and desktop/mobile visual checks passed.)

- [x] Push the latest validated release to the connected Cloudflare staging project/branch and verify the deployed homepage and hero orbit without modifying the managed Manus domain, DNS, database, storage, or unrelated provider settings. (Pushed commit `069d6b48` to the connected GitHub `staging` branch; Git webhook did not trigger, so directly deployed the built Pages Functions bundle to Cloudflare project `esut-marketplace-staging` preview branch. Deployment `37d8052f` completed successfully at `https://37d8052f.esut-marketplace-staging.pages.dev` with stable alias `https://staging.esut-marketplace-staging.pages.dev`; deployed homepage loaded and rendered all six orbit cards and category links. No managed Manus domain, DNS, database, storage, or unrelated provider setting changed.)

- [x] Implement the approved Cloudinary public-media architecture: define media metadata/lifecycle boundaries, secure server-authorized uploads, moderation-aware publication, named public transformations, cleanup/reconciliation safeguards, and verified public delivery while preserving private evidence storage. (Added additive `mediaAssets` metadata table and applied migration `0022_pretty_purple_man.sql`; public listing images and explicitly public avatars now require Cloudinary, record ownership/zone/purpose/lifecycle metadata, and use bounded named transformations. Private avatars and evidence remain protected managed-storage media; public uploads no longer silently fall back.)
- [x] Add focused automated coverage for Cloudinary media authorization, ownership, validation, moderation visibility, transformation profiles, and private/public storage separation. (Extended `server/cloudinary.test.ts` for transformation profiles, public-ID parsing, and strict approved-public eligibility; updated the moderation procedure contract for the additional media lifecycle write.)
- [x] Validate the Cloudinary implementation with TypeScript, production build, automated tests, and staging/browser checks using only non-sensitive media; document provider/account limitations and migration boundaries. (Full suite passed: 67 files, 191 tests, 1 intentional skip; TypeScript and production build passed. `CLOUDINARY_PUBLIC_MEDIA_ARCHITECTURE_2026-08-25.md` documents provider boundaries, no automatic remote deletion, and the future idempotent migration path. No marketplace records, private evidence, roles, or unrelated provider settings were changed.)

- [x] Complete the authenticated administrator dashboard read-only review across overview, moderation, analytics, settings, and audit-log surfaces, recording real findings and deferred mobile limitations. (Completed in the authenticated session on 25/08/2026; findings recorded in `AUTHENTICATED_DASHBOARD_REVIEW_NOTES_2026-08-25.md`. No marketplace actions or settings were changed.)

# Marketplace discovery improvement slice

- [x] Audit and improve the public marketplace search experience using the existing real-data query, including query normalization, clear active-filter state, and truthful loading/error recovery. (Added trimmed query inputs, active-filter chips with individual and all-clear actions, accessible filter labels, truthful range validation, fetching state, result count, and a direct correction path for contradictory prices.)
- [x] Improve top-level category discovery using the current ESUT catalogue vocabulary, preserving canonical Food/ESUTChop, Accommodation, and Digital Books & Courses routes without inventing categories or listings. (The live database contains nine active top-level categories and zero child rows; the desktop category filter now wraps so all nine are visible, while mobile/tablet retain touch scrolling. Canonical Food/ESUTChop and Accommodation routing remains unchanged.)
- [x] Improve discovery filters and sorting presentation, including price ordering, newest-first ordering, category selection, and mobile-friendly controls backed by server-calculated results. (Preserved the server-calculated sort/filter contract, added explicit sort/filter labels and active state, and verified controls at desktop, tablet, and mobile widths.)
- [x] Improve honest no-results and empty states with useful search/category recovery paths while avoiding fabricated products, reviews, ratings, or testimonials. (Kept real suggestion links and honest category fallbacks, added a specific invalid-price correction state so contradictory filters cannot appear as a misleading no-results response, and introduced no fabricated marketplace content.)
- [x] Improve product-detail discovery continuity with clear seller/category context, related real listings, and explicit return/browse paths. (Added a direct linked `{category} listings` breadcrumb above the product title; existing Back to marketplace, Visit store, View category, and related real listing paths remain intact.)
- [x] Validate marketplace discovery at desktop, tablet, and mobile viewports with TypeScript, Vitest, production build, and visual checks; document verified gaps and complete the slice checkpoint. (Focused tests pass; full suite: 67 files, 193 passed, 1 intentional skip; TypeScript and production build pass; `/explore` and a real product route were visually checked at 1280px, 768px, and 375px. Findings recorded in `MARKETPLACE_DISCOVERY_AUDIT_2026-08-25.md`.)

- [x] Prevent invalid non-empty Naira filter input from silently falling through to an unfiltered marketplace query; show a specific correction state and preserve truthful result semantics. (Non-empty non-finite, negative, and zero values now disable the query and show an actionable correction state; focused regression coverage, TypeScript, and build pass.)

# Buyer experience improvement slice

- [x] Audit real buyer account, cart, checkout, order, saved-search, notification, dispute/reporting, profile/security, and responsive flows for verified usability and safety gaps. (Authenticated production review covered account, cart, saved searches, and notifications; local protected-route review covered account, cart, checkout, orders, support, and security at desktop/mobile boundaries. Findings recorded in `BUYER_EXPERIENCE_AUDIT_2026-08-25.md`.)
- [x] Improve buyer account and order visibility with clear status, identifiers, seller context, safe return paths, and truthful loading/error/empty states. (Added account loading semantics, order-list loading skeleton/navigation continuity, and persistent account/cart/marketplace escape paths while preserving server-private order identifiers.)
- [x] Improve cart and checkout clarity around campus pickup, cash-on-pickup policy, stock/price rechecks, totals, and actionable validation without changing payment policy. (Added cart/checkout escape paths, bounded pickup-note guidance, explicit placing state, and a truthful catalogue recovery state; server-side revalidation, idempotency, and cash-on-pickup policy remain authoritative.)
- [x] Improve buyer saved-search and notification management with clear criteria, delivery preference state, and honest empty/error handling using existing real records. (Preserved real alert criteria, edit/remove and email/in-app controls, added direct account return links, and verified the real zero-alert plus one-read-notification states without mutating records.)
- [x] Improve dispute/reporting and profile/security entry points with clear evidence expectations, ownership boundaries, and safe navigation without exposing private data. (Added direct account links to support/profile/security pages and protected order-detail links from real buyer disputes; existing evidence and privacy boundaries remain unchanged.)
- [x] Validate buyer improvements at desktop, tablet, and mobile viewports with Vitest, TypeScript, production build, and visual checks; document verified gaps and checkpoint the slice. (Focused contracts pass; full suite: 68 files, 198 passed, 1 skipped; TypeScript and production build pass; local protected-route checks were run at 1280px and 375px, and authenticated production account/alert/notification routes were reviewed. Audit recorded in `BUYER_EXPERIENCE_AUDIT_2026-08-25.md`.)

# Security, routing, legal, SEO, and reliability remediation from pasted_content_2.txt

- [x] Read the complete security brief and map every finding to the existing ESUT Marketplace routes, procedures, schema, storage, authentication, and deployment configuration. (Completed from `pasted_content_2.txt`; mapping recorded in `SECURITY_REMEDIATION_AUDIT_2026-08-25.md`.)
- [x] Audit authentication, authorization, session handling, admin boundaries, URL/API ownership checks, input validation, unsafe DOM sinks, routing, error handling, headers, CORS, cookies, caching, legal links, SEO, and responsive behavior; record verified findings before fixing. (Initial repository and runtime audit is recorded in `SECURITY_REMEDIATION_AUDIT_2026-08-25.md`; exhaustive admin/session/input matrices remain explicitly separated as pending rather than overclaimed.)
- [x] Verify and remediate password recovery, support/contact routing, and token/session safety only where the current implementation does not already satisfy the brief; preserve the existing secure flow and add regression coverage. (Generic reset responses, one-hour expiry messaging, missing-token recovery, server token validation, successful reset, email-verification boundaries, and post-reset auth refresh are covered by existing UI/procedure tests; ordinary reset-email delivery remains intentionally gated by the current provider flag and is not represented as live.)
- [x] Verify and remediate XSS and unsafe user-controlled rendering across search, listings, stores, reviews, messages, reports, descriptions, URL parameters, and error states with payload-based regression tests. (Source audit found no `dangerouslySetInnerHTML`, `innerHTML`, `outerHTML`, `insertAdjacentHTML`, or `document.write` in the inspected user-content pages; React text-rendering contracts cover Explore, Product, Store, AccountFeature, and Messages; a markup-looking description payload is preserved as plain data. No unsafe sanitizer/blacklist was added.)
- [x] Verify server/API-backed protection for every admin route and procedure, including anonymous, customer, seller, administrator, stale-session, and manipulated-role cases; add regression coverage for any verified gap. (Inventory contract covers all known admin router surfaces: ordinary operations use `adminProcedure`, password replacement uses `superAdminProcedure`; endpoint tests reject signed-out, CUSTOMER, SELLER, SUPPORT, MODERATOR, malformed-role, and client-supplied role-like access; SDK contracts cover inactive/revoked tracked-session rejection; ADMIN/SUPER_ADMIN access and SUPER_ADMIN-only controls are covered. This is a code-level regression baseline, not an external penetration test.)
- [x] Verify functional terms and privacy pages, footer/registration links, last-updated metadata, truthful legal wording, unknown-route behavior, API error boundaries, and safe production recovery states. (Public legal/support/contact routes, registration/footer links, last-updated notices, unknown product/store recovery, protected admin messaging, API response boundary smoke checks, and light-only 404 recovery were verified; formal owner/legal approval remains separate.)
- [x] Verify defensive input validation, duplicate submission/idempotency, pagination, sorting/filter bounds, image/media constraints, upload ownership, order tampering protections, and database query safety against the brief. (Baseline verified through existing procedure tests plus the new checkout/order source contract: UUID idempotency, authenticated cart/order scope, atomic inventory reservation, encrypted pickup-code storage, bounded quantities, seller ownership, pagination/filter bounds, media constraints, and parameterized Drizzle query patterns. This is not an external penetration test or exhaustive fuzzing exercise.)
- [x] Verify SEO essentials, security headers, CORS/cookie policy, cache behavior, observability transport behavior, responsive routes, and production build behavior without changing paused hosting/domain/provider decisions. (Verified public-only robots/sitemap controls, route-aware metadata, Express headers, host-scoped HttpOnly cookie policy, no wildcard credentialed CORS, same-origin credentialed tRPC transport, no-store API/OAuth/staging-proxy responses, noindex staging API responses, telemetry recursion/backoff guards, and responsive production builds. This is a code/runtime baseline, not an external security scan.)
- [x] Add or update focused security, authorization, routing, XSS, legal, SEO, reliability, and responsive regression tests; run the full suite, TypeScript, production build, and visual verification before checkpointing. (Added security-header, anonymous-authorization, XSS payload, legal-route, crawler, duplicate-render, and light-only 404 contracts; full suite passes at 70 files, 208 tests, 1 intentional skip, with TypeScript/build and desktop/mobile visual checks passing.)

## Verified first remediation batch

- [x] Remove the confirmed duplicate Active listings render from `StorePage.tsx` at the component-logic level and add a regression contract. (One `STORE CATALOGUE`/`Active listings` render remains; contract coverage passes.)
- [x] Add truthful public `/terms`, `/privacy`, `/support`, and `/contact` routes with last-updated metadata, owner-reviewable wording, footer/registration navigation, and light-only responsive presentation. (All four routes are registered, linked from the storefront footer and registration consent copy, and visually verified at desktop/mobile widths.)
- [x] Add non-breaking runtime security headers in the current Express entrypoint, including transport-aware HSTS, content-type protection, referrer policy, permissions policy, and clickjacking protection; do not introduce broad credentialed CORS. (Applied and tested `nosniff`, strict-origin referrer policy, restrictive permissions policy, `frame-ancestors 'none'`, forwarded-HTTPS HSTS, Express fingerprint removal, and no-store `/api/trpc`; no wildcard credentialed CORS was added.)
- [x] Add regression coverage for the first batch: legal route availability/link targets, duplicate store-section rendering, safe text rendering for attack payloads, protected admin procedures, and unknown product/store recovery states. (Contract coverage passes in the 70-file suite; source contracts protect legal links, duplicate prevention, React text rendering, admin boundary presence, crawler files, and light-only 404 behavior.)

- [x] Prevent browser and shared-proxy caching of OAuth callback/auth responses while preserving the existing cookie and redirect flow; add a focused runtime regression. (Added no-store middleware before OAuth route registration and a source regression; full suite, TypeScript, and production build pass.)

# Messaging-first modern app improvement slice

- [x] Audit the current buyer-seller messaging route, UI states, tRPC procedures, realtime/polling behavior, privacy boundaries, and seller-workspace entry points. (Audited `MarketplaceMessagesPage`, typing/polling procedures and tests, seller dashboard entry points, and protected access boundaries; findings recorded in `MESSAGING_SELLER_AUDIT_2026-08-26.md`.)
- [x] Redesign the messaging workspace with a modern light-only conversation list, search/filter context, unread hierarchy, accessible message bubbles, timestamps, seller/buyer identity, and listing/order context using only real records. (Added private inbox context, privacy status, stronger active-row hierarchy, conversation activity state, private-thread status, improved surfaces, and preserved only real participant/listing/message data. No unread count was fabricated because the current contract does not expose one.)
- [x] Improve composer interaction states with typing feedback, sending/failed/retry behavior, disabled/loading states, keyboard accessibility, and truthful empty/loading/error recovery without fabricating messages. (Existing typing TTL/polling, send loading/error feedback, retry states, Enter/Shift+Enter behavior, max-length guidance, and no-message recovery remain intact and are covered by focused contracts.)
- [x] Improve responsive behavior so the conversation list and active thread work cleanly on desktop, tablet, and narrow mobile screens with explicit navigation back to the seller/account workspace. (Responsive styles were refined and entry boundaries were visually checked at 1280px and 375px; no horizontal overflow was observed. A real authenticated thread was unavailable in the current session, so thread-level browser behavior remains procedure/test covered.)
- [x] Polish seller-workspace messaging entry points and conversation context without weakening conversation membership or seller authorization checks. (Added an `Open messages` seller quick action to the existing protected `/seller/messages` route; seller approval boundary and membership checks are unchanged.)
- [x] Add focused messaging and seller-workspace regression tests; run full Vitest, TypeScript, production build, and desktop/tablet/mobile visual verification before checkpointing. (Added messaging hierarchy and seller quick-action contracts; complete suite: 71 files, 213 passed, 1 intentional skip; TypeScript/build pass; desktop and mobile visual checks pass, with the authenticated-thread limitation documented.)

# Unknown-store production probe

- [x] Reproduce the production `/store/elon-16350002` response, inspect runtime/proxy evidence, and confirm whether the 502 is transient or code-reproducible.
- [x] If a code defect is confirmed, fix unknown-store recovery so missing stores return a truthful not-found state rather than 502, with focused regression coverage and production verification; otherwise document the non-reproducible transient without changing marketplace data.

- [x] Reconcile the reported production unknown-store HTTP 502 against the already-resolved legal, SEO, duplicate-store, and account-recovery remediation record; keep the probe read-only until reproducibility and cause are confirmed.

# Reported security and recovery follow-up

- [x] Remove or protect the publicly visible admin-only moderation content and `/admin/listings` link on the public landing experience without weakening actual admin route guards. (Removed the stale product-moderation banner and legacy evidence-queue wording from `AdminPage`; protected admin metrics and route guards remain.)
- [x] Restore a truthful public password-recovery path while email delivery remains intentionally paused, without exposing account existence or inventing an unverified contact channel; document the provider-dependent completion boundary. (Deferred by the owner; reset machinery remains implemented, while ordinary-recipient delivery/support stays paused pending an authorized sender domain or provider.)
- [x] Make unavailable product slugs resolve to a clear not-found/recovery state instead of an HTTP 200 empty content area. (ProductPage now disables retries for typed NOT_FOUND responses and renders the unavailable-listing recovery state.)
- [x] Make unavailable store slugs resolve to a clear not-found/recovery state without an indefinite loading presentation. (StorePage now disables retries for typed NOT_FOUND responses and renders Store unavailable with a browse action.)
- [x] Ensure contradictory marketplace price filters are rejected or surfaced with an actionable validation state instead of silently returning unfiltered results. (ExplorePage now hydrates min/max URL parameters and surfaces the existing correction state before querying.)
- [x] Verify search-query rendering in a real browser and preserve script-like input as escaped plain text, with regression coverage for the rendered-content contract. (React rendered the script-like query as visible text; source contracts retain the no-raw-HTML marketplace-content boundary.)
- [x] Run focused and full regression tests, TypeScript, production build, responsive verification, and checkpoint the completed remediation without changing paused infrastructure or email-provider settings. (Focused contracts, full Vitest suite, TypeScript, production build, and desktop verification passed; paused email/infrastructure settings were unchanged.)

# Preview connectivity follow-up

- [x] Restore the managed development preview connection refused by `3000-isyid2piwrgzy5lu4skpl-ba902a1a.us4.manus.computer`, verify the Preview panel can load the homepage, and preserve published-site behavior. (Restarted the managed dev service, confirmed localhost:3000 returns HTTP 200, refreshed the preview proxy, and verified the homepage renders.)

# Authorized pasted_content_3 local fixes

- [x] Update security headers to the authorized baseline: HSTS max-age 31536000 without includeSubDomains/preload, nosniff, DENY/frame protection, strict-origin referrer policy, and a compatible CSP baseline with default-src, object-src, and base-uri. (Implemented and verified through the forwarded-HTTPS response.)
- [x] Make anonymous `/seller` show the standard account-style gate with Log in and Create account actions while preserving authenticated seller access. (Implemented and visually verified.)
- [x] Sanitize marketplace search input at the URL/UI boundary by trimming, removing control characters, and capping `q` at 100 characters while preserving escaped React rendering. (Implemented for URL hydration and live input, with a 100-character max length and focused regression coverage.)
- [x] Remove `/forgot-password`, `/reset-password`, and `/verify-email` from robots.txt while retaining protected-route disallows and the sitemap reference. (Implemented and covered by regression assertions.)
- [x] Add focused regression coverage and run full validation for the four authorized fixes; leave password-reset delivery disabled and documented as deferred. (12 focused contracts passed; full suite passed with 71 files, 220 passed, 1 skipped; TypeScript, production build, header probe, and browser checks passed.)

# Product and store not-found recovery

- [x] Replace product soft-fail behavior with a proper accessible 404-style state, helpful not-found message, and marketplace recovery navigation. (Implemented explicit 404 Listing not found state with explanation, Browse all listings, and Return home actions.)
- [x] Replace store soft-fail behavior with a proper accessible 404-style state, helpful not-found message, and marketplace recovery navigation. (Implemented explicit 404 Store not found state with explanation, Browse the marketplace, and Return home actions.)
- [x] Add focused product/store regression coverage and verify unknown slugs at desktop and mobile widths before checkpointing. (Focused contracts and ProductPage tests passed; full suite, TypeScript, build, and desktop/mobile screenshots passed.)

# Search-chip reflection remediation

- [x] Reproduce the script-like query in the real browser and inspect the rendered DOM/source behavior. (Browser verification confirmed the query was previously visible as raw angle-bracket text in the chip.)
- [x] Harden the active search-filter chip so script-like input is inert plain text and visibly escaped without changing query semantics. (Added entity escaping for ampersand, angle brackets, quotes, and apostrophes in visible query summaries; underlying search input remains normalized and unchanged.)
- [x] Add focused regression coverage, run the full validation suite, verify desktop/mobile behavior, and checkpoint the remediation. (Focused contract, full Vitest suite, TypeScript, production build, and browser verification passed.)

# Control-only administrator role

- [x] Audit administrator routes, navigation, and buyer/seller procedures to identify every capability that must be control-plane-only. (Audited buyer, favorites, reminders, search alerts, offers, messaging, reviews, cart, checkout, orders, seller self-service, account, and storefront navigation.)
- [x] Enforce server-side restrictions so ADMIN and SUPER_ADMIN accounts cannot buy, sell, create listings, use carts, checkout, or operate seller tools. (Added `marketplaceUserProcedure`, applied it to commerce routes, restricted `sellerProcedure` to SELLER, and preserved rate limiting.)
- [x] Remove buyer and seller actions from the administrator interface while preserving monitoring, moderation, management, audit, notification, settings, and security operations. (Admins receive control-center states on account/seller routes; storefront Cart and Buy / Sell actions become Control center for administrators.)
- [x] Add authorization and UI regression coverage, run the full validation suite, and checkpoint the control-only administrator release. (71 test files passed, 224 tests passed, 1 skipped; TypeScript and production build passed.)

# Admin control-center expansion

- [x] Add a protected real-data user management dashboard to view, suspend, and delete user accounts with server-side authorization, safeguards, and audit logging. (Added server-guarded suspend and privacy-preserving delete/anonymization; deletion revokes sessions/tokens, suspends owned stores/listings, preserves order/audit history, and blocks self/super-admin deletion.)
- [x] Add a protected real-data product moderation queue to review new seller listings and approve or reject them with server-side state transitions and audit logging. (Added PENDING_VALIDATION queue filter and audited approve/reject mutation with media approval, search-alert notification, and seller notification.)
- [x] Add a protected real-data analytics overview page showing total sales, active users, and new listings with truthful empty/loading/error states. (Extended the existing date-filtered analytics procedure and overview cards with completed sales, active users, and new listings.)
- [x] Add admin navigation, focused procedure/UI regressions, responsive verification, full validation, and checkpoint the admin control-center expansion. (Admin navigation already included Users, Moderation, and Analytics; focused tests passed, full suite passed with 71 files / 225 tests / 1 skipped, TypeScript and build passed, and desktop/mobile protected-route checks passed.)

# Aiven service architecture comparison

- [x] Analyze `pasted_content_4.txt` and compare PostgreSQL, Kafka, OpenSearch, ClickHouse, and Valkey with ESUT Marketplace’s current MySQL/TiDB, Redis, Cloudinary, private storage, and managed-host architecture; make no infrastructure changes without explicit authorization. (Completed in `AIVEN_SERVICE_COMPARISON_2026-08-26.md`; no infrastructure or data changes made.)

# Redis health verification

- [x] Verify whether the configured Redis service is reachable and actively used by ESUT Marketplace for lockouts, rate limits, telemetry limits, or other short-lived security state, without changing data or credentials. (Endpoint TCP/TLS reachability confirmed, but application-native PING reports `CONNECTION_FAILED`; the configured scheme is malformed as `redis-cli--tls-uredis:` and the backend is using its conservative database fallback.)
- [x] Correct the server-side `REDIS_URL` secret to a valid TLS Redis URI (for example, `rediss://...`) and rerun the application-native PING/health check; do not expose or commit the credential. (Updated through the managed secret input; exact credential remained private; restart loaded the value and health returned reachable with throttling available.)

# Upstash Redis replacement

- [x] Replace the malformed current Redis configuration with an authorized Upstash Redis connection for server-side security state, preserving TLS, key namespaces, TTLs, rate-limit behavior, fallback handling, and secret privacy; validate before checkpointing. (Upstash `rediss://` URI supplied through managed secrets; server-only PING passed; application health reports configured/reachable/throttlingAvailable; atomic EVAL lockout sequence passed with unique cleanup keys.)

# Mobile hero artwork overlap

- [x] Adjust the mobile hero artwork so the Campus Deals label does not cover the five category elements, while preserving desktop composition and ESUT branding; verify both breakpoints before checkpointing. (Added a mobile-only collision guard that reduces the caption footprint, moves it into the lower-right safe area, and preserves desktop styling; mobile and desktop screenshots, focused tests, TypeScript, and production build passed.)

# Final category taxonomy and Coming Soon enforcement

- [x] Inventory the current nine official category records, stable IDs/slugs, existing listings, saved searches, routes, and all public discovery/write boundaries without deleting, renaming, recreating, or reassigning data. (Verified current IDs/slugs 1/2/3/4/5/6/7/8/30001; only active retained-category listing is id 3, `engineering-mathematics-textbook`, in category 30001; Food/Accommodation have no linked listings; one saved search has no category filter.)
- [x] Implement one server-authoritative ACTIVE/COMING_SOON category availability policy that keeps Electronics, Phones & Accessories, Computing, Fashion, Beauty & Personal Care, and Services active while marking Food, Books & Academic, and Hostel & Lodge as Coming Soon using verified persistent identifiers. (Added shared `categoryAvailability` policy keyed by preserved IDs and canonical/legacy slugs; no category rows, IDs, names, listings, orders, or saved-search records were changed.)
- [x] Prevent Coming Soon categories and their preserved listings from reaching any public discovery path, including homepage shelves, search, filters, suggestions, direct product access, related products, public stores, feeds, recommendations, alerts, pagination, and manually edited URLs. (Applied reusable public-category predicate to homepage, search, suggestions, product, related listing, public store products/reviews, alerts, offers, and carts; searches for future categories short-circuit before inventory queries; direct preserved-listing URL now resolves to the existing truthful not-found recovery state.)
- [x] Create a branded, responsive, accessible Coming Soon experience for Food / ESUT Chop, Books & Academic / Digital Academic Resources, and Hostel & Lodge / ESUT Accommodation across category navigation, hero/cards, Explore, dedicated vertical routes, legacy routes, and direct links. (Generic `/category/*` state presents available-category recovery; `/esutchop`, `/accommodation`, and legacy hostel paths route into it; header, Explore chips, homepage tiles, and hero tiles visibly identify future categories as Coming Soon.)
- [x] Block sellers from creating, updating, or publishing listings in Coming Soon categories at both the category-selector UI and authoritative server boundaries, without silently changing a seller’s chosen category. (Seller select disables future categories and labels them Coming Soon; existing retained listings show a clear preserved-state warning; create/update/publish reject bypass requests server-side.)
- [x] Preserve protected administrator visibility of retained historical category records, listings, orders, audits, and moderation data without reusing public-discovery filtering or expanding admin permissions. (No admin procedure was narrowed, no records were deleted/archived/reassigned, and public predicates are scoped only to public/buyer discovery and new buyer actions.)
- [x] Add focused policy, public-query, bypass, seller-boundary, preservation, UI/accessibility, responsive, and administrator-history regressions; run full validation and checkpoint only after all acceptance criteria pass. (Added policy/enforcement contracts; full suite and production build passed; desktop and 375px captures verified three Coming Soon states, active discovery, and direct hidden-listing recovery.)

# Approved final sub-category hierarchy

- [x] Inventory and document the current category table, parent-child relationships, listing assignments, seller-form inputs, and public category route dependencies before adding the approved hierarchy; do not delete, rename, or reassign existing records during the inventory. (Verified the preserved nine parent records and ten pre-existing listing assignments before migration; no parent records or listing category IDs were changed.)
- [x] Add the exact approved child categories under the six active parent categories—Electronics, Phones & Accessories, Computing, Fashion, Beauty & Personal Care, and Services—with stable slugs, sort order, parent relationships, and no unapproved generic additions. (Inserted the exact 61 approved active children using an idempotent migration: Electronics 10, Phones 10, Computing 14, Fashion 12, Beauty 7, Services 8.)
- [x] Add the retained official Coming Soon child categories ESUT Chop, Digital Academic Resources, and ESUT Accommodation under their existing future parent categories without making their inventory discoverable or sellable. (Inserted exactly three retained children under Food id 6, Digital Books & Courses id 30001, and Accommodation id 5; public direct routes resolve to the shared Coming Soon state.)
- [x] Extend category availability and public-query safeguards so a child category inherits the status of its top-level parent; preserve historical listing records and admin visibility while excluding retained-category descendants from public discovery and commerce actions. (Shared availability accepts parentId; public listing predicate excludes parent/child Coming Soon inventory; suggestion metadata carries parentId; existing protected administrator queries and records remain unmodified.)
- [x] Add parent-and-child category selection to seller create/edit workflows, requiring an active child category for new listings and rejecting bypassed, mismatched, inactive, or Coming Soon category combinations server-side. (Dedicated seller classification feed exposes approved leaf categories with parent labels; new creates require matching parent/child IDs; update preserves legacy edits safely while rejecting mismatches and retained future categories.)
- [x] Add responsive active-category navigation and nested browsing/filter UX that exposes approved sub-categories without cluttering the homepage; retain honest Coming Soon pathways for Food, Books & Academic, and Hostel & Lodge. (Header/home retain compact parent navigation; Explore reveals horizontal sub-category chips when a parent or child is selected; desktop and 375px mobile paths verified.)
- [x] Add schema, migration, procedure, policy, hierarchy-integrity, seller-authorization, public-visibility, and mobile/desktop regression coverage; validate the full project and checkpoint the completed hierarchy release. (Drizzle generation confirmed no schema change; non-destructive data migration applied; category policy/enforcement/seller tests, full suite, TypeScript, production build, database verification, desktop, and mobile captures passed.)

# Product detail retrieval regression

- [x] Reproduce and repair the product-detail retrieval failure at `/product/iphone-15-pro-max-010c0bf3`, add a focused regression, and verify the exact route plus related active listings without changing marketplace records. (The hierarchy predicate referenced `categories.parentId` in the related-products query without joining `categories`, producing the real 500 response. Made the predicate join-aware, kept full parent/child protection where categories is joined, used the direct-parent condition only for related listings already constrained to the verified active product’s category, joined categories in suggestions, and added a regression contract. The exact iPhone 15 Pro Max route now renders the listing and related products; focused tests, full suite, TypeScript, and production build passed.)

# Administration control-center review

- [x] Audit the control-only administrator routes, navigation, real-data dashboards, user controls, moderation, analytics, security actions, and responsive states; identify concrete improvements without expanding admin commerce permissions or fabricating data. (Reviewed the protected AdminSuite routes and admin procedure surface. Current real-data control center includes Overview, analytics, users with deactivation/privacy-preserving deletion, seller/store controls, listing and evidence moderation, verifications, categories, orders/reservations/offers, reports/disputes/reviews, notifications, audit logs, recovery, settings, and super-admin staff roles. All visual action paths use role gates and reason capture; the primary improvement opportunity is workflow-focused prioritization and navigation—not buyer/seller commerce access.)

# Unified general administration control center

- [x] Redesign the administrator experience into one coherent, general marketplace control center with grouped navigation, a real-data command overview, contextual operational queues, and linked management workspaces, while preserving control-only administrator permissions and truthful data. (Added a shared protected `AdminControlCenterNav` across every `/admin` route, replacing the duplicate flat navigation on suite pages. It groups the established real-data workspaces into Command Center, Marketplace Management, Trust & Safety, and System Control; shows Staff roles only to SUPER_ADMIN; retains existing operational overview/drill-down links; and contains no cart, selling, or commerce routes. The admin authorization suite, analytics tests, navigation contract, full Vitest suite, TypeScript, and production build passed. Desktop/mobile signed-out captures confirm the control center remains hidden behind the existing administrator boundary; direct authenticated visual review requires an administrator session.)

# Public product-media delivery investigation

- [x] Recheck why some public listing images fail to render, inventory live image metadata and delivery URLs, repair only the verified public-storage/media fault, and validate representative listings without exposing private evidence or altering unrelated marketplace data. (Read-only audit on 2026-08-27 found four active listings, each with a primary Cloudinary URL; all four returned HTTP 200 image/jpeg, and all four public product routes returned HTTP 200. No active listing lacked a primary image, no verified delivery fault remained, and no data was mutated. Two older active Cloudinary rows lack matching mediaAssets metadata and are documented as a future idempotent reconciliation observation; secondary legacy gallery rows were preserved.)

# Reference-inspired category menu redesign

- [x] Redesign the ESUT Main Category Menu as a clean icon-led vertical category drawer/dropdown matching the approved reference pattern: show only the six active parent categories at first level, reveal approved active children progressively, exclude Food, Books & Academic, and Hostel & Lodge from the drawer, and preserve all existing Coming Soon routes, data, and safeguards. (Added a shared icon-led Main Category Menu with a desktop dropdown trigger and mobile direct-open drawer. It shows only active parents, progressively reveals active children, filters Coming Soon parents/children from navigation, uses real category routes, includes keyboard-visible focus states and accessible labels, and replaces the crowded horizontal category row. Focused menu/storefront/category tests, TypeScript, full suite, production build, and 375px homepage/Explore screenshots passed.)

# Permanent mock-data cleanup

- [x] Identify and classify development/mock seller accounts, stores, and products using verified development evidence; map dependent records and preserve genuine sellers, listings, historical orders, audit logs, and private evidence. (Verified users 5/6/7—ESUT Tech Hub, Campus Living, and Study Corner—were created at the original seed timestamp, had no seller applications or public media, and owned listings 1–6; genuine approved sellers were newer authenticated records and excluded.)
- [x] Permanently delete the explicitly authorized mock seller accounts, stores, products, linked cancelled orders/order items, and safely removable dependents using the reviewed deletion scope; verify no genuine marketplace data is affected. Preserve unrelated genuine sellers, listings, orders, audit logs, and private evidence. (Deleted users 5/6/7, stores 1/2/3, listings 1–6, five cancelled orders 1/2/30001/30002/60001, their order items/reservations/status history/batches, listing media/inventory, personal notifications and account records, favorites, reminders, and other safely linked dependents in one transaction. Preserved audit logs and case evidence. Post-delete checks returned zero for every targeted entity; the public catalogue now shows four genuine listings from Bernard Raphael, Blythe Pingree, and Elon Musk.)

# Preview panel connectivity regression

- [x] Diagnose why the managed Preview panel is not showing, repair the verified service/proxy/runtime fault, and verify preview rendering without changing production marketplace data or deferred infrastructure settings. (The application was healthy before the incident, but the managed dev process exited with code -1 at 10:48:54, temporarily leaving the Preview panel without a target. Restarted the managed service; it initialized OAuth, returned `Server running on http://localhost:3000/`, localhost health returned HTTP 200, and the managed preview URL rendered the ESUT Marketplace homepage. No application code, marketplace data, or deferred infrastructure settings changed.)

# Repeated Preview panel refusal

- [x] Reproduce the embedded Preview panel refusal after the prior restart, identify why the managed preview target stops accepting connections, stabilize the service or proxy, and verify the embedded target without changing application data. (The screenshot represented a transient managed-preview outage: the dev process had exited with code -1 during repeated prior restart cycles, so the embedded frame temporarily refused the fixed preview hostname. After the managed service restart, the process remained running, localhost returned HTTP 200, the exact public preview host returned HTTP 200, and the preview capture rendered the homepage successfully. No application code or marketplace data changed.)

# Mobile hero Campus Deals overlap regression

- [x] Repair the mobile-only Campus Deals label overlap with the Food & Snacks hero chip, preserve desktop composition and category data, and verify all five hero chips at mobile and desktop breakpoints. (Moved the mobile caption from the lower-right chip area to a centered top safe zone, reduced its mobile footprint, and preserved desktop positioning. Full 375px mobile capture confirms the caption no longer covers Food & Snacks or the other hero chips; desktop capture remains unchanged. TypeScript, full test suite, production build, and responsive verification passed. A transient Redis fallback warning appeared during one preview capture and is unrelated to the CSS-only hero change.)

# Mobile hero caption position refinement

- [x] Restore Campus Deals to its previous lower-right mobile position and reduce only its size enough to keep the Food & Snacks chip unobstructed; preserve desktop composition and verify responsive behavior. (Restored `right: 4%` and `bottom: 2.5%`; reduced the mobile footprint from 38% to 34% width and from a 7.6vw maximum scale to a 6.4vw scale. Mobile full-page and desktop captures passed visual review; TypeScript, full Vitest suite, and production build passed.)

# Verified-purchase review experience refinement

- [x] Analyze the supplied verified-purchase review reference against the current product/store review UI and define truthful, light-only improvements using only genuine buyer reviews and real seller responses. (Reference analysis and current ProductPage audit completed; the design keeps the red verification eyebrow, strong heading, white rounded card, gold rating, real buyer identity, review text, and conditional seller response while adding stronger app-style hierarchy.)
- [x] Implement the approved verified-purchase review presentation improvements, including clear verification, readable rating/comment hierarchy, and an optional seller-response treatment without fabricating review content. (Added modern light-only cards with buyer initials derived from the real buyer name, accessible five-star labels, verified-purchase badges, responsive comment blockquotes, rating summary, gradient accent, hover polish, and a distinct seller-response panel rendered only from `review.sellerResponse`.)
- [x] Add focused review regression coverage, run TypeScript, full Vitest, production build, responsive visual verification, and checkpoint the review release. (Added ProductPage review source regressions for real review fields, accessible rating semantics, truthful empty state, and no reference-demo content; 75 test files, 235 tests passed, 1 skipped; TypeScript, production build, and real product desktop/mobile screenshots passed.)

- [x] Modernize the verified-purchase review cards to feel more like a polished app while preserving the reference hierarchy, real review data, seller-response truthfulness, accessibility, and light-only ESUT styling. (Implemented in `ProductPage.tsx` with no fabricated reviews, ratings, buyers, or seller responses.)

# Review sorting and mobile layout refinement

- [x] Add an interactive real-data review sort control for highest rating, lowest rating, newest, and oldest review order. (Added a local deterministic sort control with Newest first, Oldest first, Highest rated, and Lowest rated options using the existing real review rating and createdAt fields.)
- [x] Optimize the verified-purchase review cards and controls for narrow mobile screens while preserving readable buyer, rating, review, and seller-response hierarchy. (The sort select becomes full width on mobile; card padding, avatar sizing, comment typography, response spacing, and rating pill were tightened without overflow.)
- [x] Add focused review sorting/layout regressions, run full validation, visually verify desktop/mobile product pages, and checkpoint the release. (Hook-order regression found by mobile capture was corrected; focused ProductPage tests, TypeScript, full Vitest suite, production build, and real product desktop/mobile captures passed.)

# Review summary pill removal

- [x] Remove the redundant aggregate rating summary pill above the review cards while retaining interactive sorting and individual verified review ratings. (Removed the highlighted aggregate summary pill from `ProductPage.tsx`; retained the full-width mobile/compact desktop sort control and each review’s individual accessible rating. Focused ProductPage test, TypeScript, full Vitest suite, production build, and 375px mobile visual verification passed.)

# Compact buyer-name review rows

- [x] Adapt verified-purchase reviews into compact social-commerce rows using initials derived from the real buyer name when no genuine avatar asset exists, while retaining real rating, date, comment, sorting, and seller-response data. (ProductPage now derives up to two initials from the real buyer name, displays the real localized review date, preserves individual rating/sorting/comment/seller-response fields, and does not fabricate avatar assets.)
- [x] Add focused regressions, run full validation, visually verify mobile and desktop product pages, and checkpoint the compact review-row release. (Added deterministic helper tests for buyer initials and date formatting; focused ProductPage tests, TypeScript, full Vitest suite, production build, and real product mobile/desktop captures passed.)

# Modern review visual redesign

- [x] Replace the overly compressed review row with a higher-quality modern app review component, improving spacing, alignment, card hierarchy, mobile readability, and visual polish without changing review truthfulness or eligibility. (Refined `ProductPage.tsx` with a larger initials avatar, stronger buyer identity hierarchy, soft ESUT-green avatar treatment, warm gold rating chip, spacious comment block, polished seller-response surface, and responsive card spacing.)
- [x] Add or update focused review regressions, run full validation, visually verify the redesign at mobile and desktop widths, and checkpoint the release. (Focused ProductPage tests passed: 7 tests; TypeScript passed; full Vitest suite and production build passed; real product mobile and desktop screenshots confirmed the redesigned card renders without overflow.)

# ESUT review card colour refinement

- [x] Add a quality energetic ESUT Marketplace colour treatment to the verified-purchase review cards with readable contrast, restrained green/red/gold accents, and no change to review data or light-only behavior. (Added a soft ESUT-green card surface, restrained red-to-gold-to-green top accent, warm gold rating chip, and dark readable review text without changing review data or light-only behavior.)
- [x] Validate responsive presentation and contrast, run focused/full checks, and checkpoint the colour refinement. (Focused ProductPage tests, TypeScript, full Vitest suite, production build, and real product 375px mobile/1280px desktop visual checks passed.)

# Cross-device review card verification

- [x] Verify the review cards and review sort control at phone, tablet, and desktop widths, correcting any breakpoint-specific overflow, wrapping, contrast, or spacing issue without changing real review behavior. (Verified real product page at 375px phone, 768px tablet, and 1280px desktop widths. No overflow, clipping, contrast, or spacing defect required correction; phone uses stacked full-width sorting, tablet transitions cleanly, and desktop aligns beside Buyer Guidance.)
- [x] Add responsive regression coverage, run full validation, and checkpoint the cross-device review verification. (Added a focused responsive ProductPage source contract; focused tests, TypeScript, full Vitest suite, production build, and three breakpoint captures passed.)

# Approved admin control-center implementation

- [x] Refine the shared admin control-center shell with persistent grouped navigation, responsive mobile drawer behavior, role context, breadcrumbs, and clear escape routes while preserving control-only permissions. (Implemented grouped sidebar/mobile drawer, route-aware desktop top bar, role context, breadcrumbs, return-to-marketplace, logout, and server-side role preservation.)
- [x] Design and implement the Command Center overview hierarchy with real headline metrics, exception workload summary, deep links, health states, and operational activity. (Mounted the real-data Needs Attention queue beside existing KPI, reminder, runtime/asset health, security alert, launch activity, verification, and application surfaces; corrected application deep links to the existing overview anchor.)
- [x] Unify admin workspace UX patterns for marketplace management, trust and safety, analytics, and system control without changing backend authorization or real data. (Applied the shared route-aware shell context, grouped navigation, consistent workspace header, Back to overview escape route, responsive styling, and shared loading/error/empty handling across the admin suite and control-plane workspaces. Fixed an existing always-truthy JSX feedback wrapper that left successful Users, Sellers, Stores, Moderation, Categories, Offers, Reports, Disputes, Reviews, Notifications, and Analytics pages blank. No authorization, marketplace records, or backend procedures were changed.)
- [x] Add focused admin shell/overview regression coverage, run TypeScript, full Vitest, production build, responsive visual checks, and checkpoint the approved admin release. (Added admin shell/route/queue/audit contracts and attention-queue authorization/order tests; 77 test files, 246 tests passed, 1 skipped in the last full run; TypeScript and production build passed; authenticated desktop overview and verification workspace plus anonymous mobile/desktop boundary states were visually checked.)

# Pasted Phase 1 admin control-center requirements

- [x] Implement the approved Phase 1 admin shell, navigation, Command Center overview, unified attention queue, responsive behavior, and shared admin UI foundation by enhancing existing code only. (Added responsive sidebar/mobile drawer, route-aware desktop top bar and breadcrumbs, public-navigation separation on `/admin` routes, real-data Needs Attention queue, queue refresh/loading/error/empty states, and overview integration without changing marketplace data or server-side role policy.)
- [x] Verify and correct `/admin/verifications` so it opens the dedicated verification workspace without weakening authorization. (Dedicated route remains protected; anonymous desktop/mobile captures expose only generic access states; analytics and queue application links now land on the real `/admin#seller-applications` anchor rather than nonexistent `/admin/applications`.)
- [x] Verify administrator verification approval/rejection creates the existing immutable audit-log record with safe metadata and no private evidence or secrets. (Existing transactional `reviewVerification` path was audited and protected by a focused contract covering action attribution, verification target type, and private-evidence exclusion from queue/history.)
- [x] Validate anonymous, buyer, seller, moderator, support, admin, and super-admin boundaries; preserve public marketplace, buyer, seller, and commerce behavior; run full tests/build and responsive QA; checkpoint Phase 1. (Existing 13-case admin endpoint security coverage rejects anonymous, CUSTOMER, SELLER, SUPPORT, and MODERATOR callers and confirms ADMIN/SUPER_ADMIN paths; public/buyer/seller behavior remained covered by the full suite. Authorized desktop admin QA passed; managed mobile screenshot used a separate unauthenticated session, so authorized mobile drawer interaction remains a documented follow-up rather than an unverified claim.)

# Approved pasted_content_8 narrow-mobile remediation

- [x] Audit and remediate shared layout/components first for root-cause narrow-mobile issues across 320/360/375/390px without global overflow suppression or application rewrite. (Audited shared global layout, public grids/category strip, fixed-width login dialog, chat shell, dashboard loading skeleton, admin shell, and media/container patterns. Corrected the public grid/category composition, made the login dialog viewport-safe with internal scrolling, and prevented the desktop dashboard skeleton sidebar from participating in mobile loading. No global overflow suppression, backend/API, schema, or authorization changes were made.)
- [x] Remediate public marketplace routes and shared mobile navigation, category drawer, hero, grids, filters, product/store detail, cart, checkout, auth, and public account entry points while preserving desktop/tablet composition. (Verified homepage, Explore, product, store, cart, checkout, login, `/sell`, and public/protected account entry routes across the required narrow-phone matrix. Corrected narrow-phone product-grid stacking, category-strip local scrolling, and shared dialog sizing; hero/static fallback and public behavior were preserved. Desktop/tablet composition remained intact.)
- [x] Remediate buyer/seller workspaces, forms, tables, messaging composer, attachments, typing state, and dashboard actions for 320px without changing mutations, API calls, or business logic. (Verified canonical `/seller`, `/seller/messages`, `/account/orders`, `/account/search-alerts`, and `/account/messages` narrow-phone states; shared chat shell already uses shrink-safe columns, wrapped message content, local scrolling, and mobile composer rules. Dashboard loading skeleton no longer exposes its fixed desktop sidebar on mobile. Authenticated interaction was not performed destructively.)
- [x] Remediate all specified admin control-center routes for narrow phones while preserving control-only role boundaries and public/admin route separation. (Captured all listed admin route groups at 320px and 390px; protected states remained contained and free of public commerce navigation. Existing authenticated desktop admin QA plus server authorization contracts cover the control-only boundary. The managed narrow-mobile captures were unauthenticated, which is documented rather than overstated.)
- [x] Add focused responsive regression contracts, run QA at 320/360/375/390/768/1280px for major routes and role contexts, run TypeScript/full tests/build, document verified limitations, and checkpoint the complete remediation. (Added `NarrowMobileResponsive.contract.test.ts`; required viewport captures completed at 320, 360, 375, 390, 768, and 1280px across public, protected buyer/seller, messaging, and admin route groups; TypeScript, focused tests, full Vitest, and production build passed. Responsive evidence and authenticated-session limitations are recorded in `NARROW_MOBILE_REMEDIATION_NOTES_2026-08-28.md`.)

# Authentication callback and narrow-mobile follow-up

- [x] Audit the public homepage callback behavior and the Manus OAuth callback contract to determine why `/?code=...` is visible and whether the code is being consumed safely. (The server callback route is correct; the visible code was a misrouted/stale SPA landing and is now documented in `NARROW_MOBILE_CALLBACK_QA_2026-08-28.md`.)
- [x] Implement the smallest safe callback handling fix, preserving nonce/state validation, session exchange, fixed redirect behavior, and avoiding code leakage in URL history/referrers. (Non-callback SPA URLs now remove callback parameters via `history.replaceState`; the server exchange and redirect remain unchanged.)
- [x] Re-verify the 320px narrow-phone homepage and major route matrix after the callback fix, with no unrelated responsive or business-logic changes. (320px representative public/gated route capture plus 360/375/390/768/1280 homepage captures passed visual review.)
- [x] Review `pasted_content_8.txt` against the current remediation and document completed coverage, verified limitations, and any remaining authorized fixes; run focused/full validation and checkpoint the release. (Audit and QA handoff documented; full tests, TypeScript, build, and checkpoint completed.)

- [x] Remove stray OAuth callback parameters from non-callback SPA URLs without bypassing server-side code/state validation. (Added client-side history cleanup for misrouted callback parameters; server callback nonce/state validation and code exchange remain unchanged.)
- [x] Re-verify the homepage and authenticated workspace route matrix at 320px, 360px, 375px, and 390px after callback cleanup. (Public homepage and representative gated buyer/seller/messaging/admin states were visually checked at 320px; homepage was checked at 360px, 375px, 390px, 768px, and 1280px. Authenticated narrow visual capture remains unavailable in the managed session.)
- [x] Audit the remaining pasted_content_8 requirements and document any unresolved narrow-mobile edge cases. (Requirements matched the already-completed remediation; no new application edge case was identified. Authenticated narrow visual capture remains the only verified limitation.)

# Logout and Security & devices follow-up

- [x] Make administrator, buyer, and seller logout controls consistently visible, easy to reach, and clear on desktop and narrow mobile. (Completed through the shared buyer/seller shell and admin control-center header, sidebar, and mobile actions.)
- [x] Repair the Security & devices active-session query and recent security activity query/mutations so real data, loading, empty, error, and refresh/revocation states work. (Added refresh and fetching feedback, active-session count and safe per-device revoke control, while preserving current-device protection and real recent activity data.)
- [x] Add focused logout/session-security regression tests, run full validation, verify responsive protected workspaces, and checkpoint the release. (Added `WorkspaceLogout.contract.test.ts`; 80 test files, 255 tests passed, 1 skipped; TypeScript, production build, and 390px protected-route captures passed.)

# Workspace logout usability follow-up

- [x] Make logout controls consistently visible and easy to reach in administrator, buyer, and seller dashboards on desktop and narrow mobile. (Buyer/seller shells now expose a persistent header action plus existing sidebar/menu actions; admin exposes desktop top-bar, mobile-bar, sidebar, and drawer actions.)
- [x] Preserve secure logout mutation behavior and safe redirect/session refresh behavior across all three workspace types. (All controls use the existing auth logout mutation, clear client auth state/session mirror, prevent duplicate clicks, and return to `/` through existing page handlers.)
- [x] Add focused logout regression coverage, run validation, verify responsive workspaces, and checkpoint the release. (Added `WorkspaceLogout.contract.test.ts`; focused tests, TypeScript, production build, and 390px `/account`, `/seller`, `/admin` boundary captures passed.)

# Mobile public-header menu removal

- [x] Remove the circled public mobile hamburger control and only its dependent trigger behavior without affecting cart or desktop navigation. (Removed the public mobile hamburger trigger and its dependent drawer; retained cart, desktop navigation, Categories, and Browse all listings.)
- [x] Verify the public header and homepage at 320px, 360px, 375px, 390px, tablet, and desktop; run tests/build and checkpoint the repair. (Focused tests, TypeScript, production build, and visual checks at 320px, 390px, 768px, and 1280px passed; 360px and 375px remain covered by the existing narrow-mobile regression history.)

# Robots crawler-policy review

- [x] Audit robots.txt directives against public indexable routes, sitemap entries, and protected application paths. (Confirmed the prior file explicitly named private `/account`, `/checkout`, `/admin`, `/moderator`, and `/seller` paths.)
- [x] Remove sensitive-path enumeration from robots.txt while preserving truthful public-route indexing and server-side authorization. (robots.txt now contains only the global allow rule and sitemap; private routes receive route-aware `X-Robots-Tag: noindex, nofollow, noarchive` headers and remain server-protected.)
- [x] Add crawler-policy regression coverage, run validation, and checkpoint the robots.txt repair. (Updated crawler/security-header tests; full suite, TypeScript, and production build passed.)
