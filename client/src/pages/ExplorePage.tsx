import { Button } from "@/components/ui/button";
import { condition, naira } from "@/lib/marketplace";
import { trpc } from "@/lib/trpc";
import { Package, Search, SlidersHorizontal } from "lucide-react";
import { Link, useLocation, useRoute } from "wouter";
import { useMemo, useState } from "react";

type Condition = "" | "NEW" | "LIKE_NEW" | "USED_GOOD" | "USED_FAIR" | "REFURBISHED";

export default function ExplorePage() {
  const [location] = useLocation(); const [, categoryParams] = useRoute("/category/:slug"); const start = useMemo(() => new URLSearchParams(location.split("?")[1] ?? "").get("q") ?? "", [location]); const [q, setQ] = useState(start); const [sort, setSort] = useState<"newest" | "price_asc" | "price_desc" | "popular">("newest"); const [verified, setVerified] = useState(false); const [conditionFilter, setConditionFilter] = useState<Condition>(""); const [min, setMin] = useState(""); const [max, setMax] = useState(""); const [page, setPage] = useState(1); const categorySlug = categoryParams?.slug; const minKobo = min && Number(min) >= 0 ? Math.round(Number(min) * 100) : undefined; const maxKobo = max && Number(max) > 0 ? Math.round(Number(max) * 100) : undefined; const reset = (fn: () => void) => { fn(); setPage(1); }; const results = trpc.marketplace.search.useQuery({ q: q || undefined, categorySlug, min: minKobo, max: maxKobo, condition: conditionFilter || undefined, sort, verified, page, limit: 24 }); const title = categorySlug ? `${categorySlug.replaceAll("-", " ")} listings` : "Find your next campus essential";
  return <main className="page-shell py-8"><p className="eyebrow text-[#00843d]">{categorySlug ? "CATEGORY BROWSE" : "MARKETPLACE CATALOGUE"}</p><h1 className="mt-2 text-3xl font-extrabold capitalize">{title}</h1><form className="mt-6 flex max-w-3xl gap-2" onSubmit={event => { event.preventDefault(); setPage(1); }}><div className="searchbox flex-1"><Search size={18}/><input value={q} onChange={event => reset(() => setQ(event.target.value))} placeholder="Search listings"/></div><Button className="bg-[#e31b23]">Search</Button></form><div className="mt-6 flex flex-wrap items-center gap-3"><label className="flex items-center gap-2 rounded-xl border bg-white px-3 py-2 text-sm font-semibold"><SlidersHorizontal size={15}/><select value={sort} onChange={event => reset(() => setSort(event.target.value as typeof sort))} className="border-0 bg-transparent outline-none"><option value="newest">Newest</option><option value="popular">Popular</option><option value="price_asc">Price: Low to High</option><option value="price_desc">Price: High to Low</option></select></label><label className="rounded-xl border bg-white px-3 py-2 text-sm font-semibold">Condition <select value={conditionFilter} onChange={event => reset(() => setConditionFilter(event.target.value as Condition))} className="ml-1 border-0 bg-transparent outline-none"><option value="">Any</option><option value="NEW">New</option><option value="LIKE_NEW">Like new</option><option value="USED_GOOD">Used — good</option><option value="USED_FAIR">Used — fair</option><option value="REFURBISHED">Refurbished</option></select></label><label className="rounded-xl border bg-white px-3 py-2 text-sm font-semibold">Min ₦<input value={min} inputMode="decimal" onChange={event => reset(() => setMin(event.target.value))} className="ml-1 w-20 border-0 bg-transparent outline-none" placeholder="0"/></label><label className="rounded-xl border bg-white px-3 py-2 text-sm font-semibold">Max ₦<input value={max} inputMode="decimal" onChange={event => reset(() => setMax(event.target.value))} className="ml-1 w-20 border-0 bg-transparent outline-none" placeholder="Any"/></label><label className="flex items-center gap-2 rounded-xl border bg-white px-3 py-2 text-sm font-semibold"><input type="checkbox" checked={verified} onChange={event => reset(() => setVerified(event.target.checked))}/>Verified sellers only</label></div>{results.isLoading ? <p className="py-12 text-slate-500">Searching marketplace listings…</p> : results.data?.items.length ? <><div className="mt-7 product-grid">{results.data.items.map(({ listing, store, image }) => <article className="product-card" key={listing.id}><Link href={`/product/${listing.slug}`}><div className="product-visual aspect-[1/.75]">{image?.url ? <img src={image.url} alt={listing.title} className="h-full w-full object-cover"/> : <Package size={43}/>}</div></Link><div className="p-4"><p className="text-xs font-bold text-[#00843d]">{store.isVerified ? "VERIFIED SELLER" : "SELLER"}</p><Link href={`/product/${listing.slug}`}><h2 className="mt-1 font-bold leading-5">{listing.title}</h2></Link><p className="mt-3 text-lg font-extrabold text-[#e31b23]">{naira(listing.priceKobo)}</p><p className="mt-2 text-xs text-slate-500">{condition(listing.condition)} · {listing.location}</p></div></article>)}</div><nav aria-label="Catalogue pages" className="mt-8 flex items-center justify-center gap-3"><Button variant="outline" disabled={page === 1} onClick={() => setPage(current => Math.max(1, current - 1))}>Previous</Button><span className="text-sm font-bold text-slate-600">Page {page}</span><Button variant="outline" disabled={!results.data?.hasMore} onClick={() => setPage(current => current + 1)}>Next</Button></nav></> : <div className="empty-panel mt-7"><Package size={34}/><p>No listings match those filters.</p></div>}</main>;
}
