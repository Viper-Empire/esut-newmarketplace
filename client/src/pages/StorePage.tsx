import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { naira } from "@/lib/marketplace";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Flag, MapPin, Package, ShieldCheck, Store, Star } from "lucide-react";
import { Link, useRoute } from "wouter";
import { toast } from "sonner";

export default function StorePage() { const [, params] = useRoute("/store/:slug"); const { isAuthenticated } = useAuth(); const storeQuery = trpc.marketplace.store.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) }); const report = trpc.support.report.useMutation({ onSuccess: () => toast.success("Report submitted for administrator review."), onError: error => toast.error(error.message) }); const reportItem = (targetType: "STORE" | "REVIEW", targetId: number) => { if (!isAuthenticated) { toast.error("Sign in to submit a report."); return; } const reason = window.prompt("Brief reason for this report:"); const details = window.prompt("Describe the concern (at least 10 characters):"); if (reason?.trim() && details?.trim()) report.mutate({ targetType, targetId, reason: reason.trim(), details: details.trim() }); };
  if (storeQuery.isLoading) return <main className="page-shell py-16">Loading store…</main>;
  if (!storeQuery.data) return <main className="page-shell py-20 text-center"><Store className="mx-auto text-slate-400" size={42}/><h1 className="mt-4 text-3xl font-extrabold">Store unavailable</h1><Link href="/explore"><Button className="mt-6 bg-[#e31b23]">Browse marketplace</Button></Link></main>;
  const { store, products, reviews } = storeQuery.data;
  return <main className="min-h-screen bg-[#f7f9fc]"><div className="page-shell py-8"><Link href="/explore" className="inline-flex items-center gap-2 text-sm font-bold text-[#00843d]"><ArrowLeft size={16}/>Marketplace</Link><section className="mt-5 rounded-3xl bg-[#111827] p-7 text-white sm:p-10"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="flex gap-4"><span className="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-[#00843d]"><Store size={28}/></span><div><p className="flex items-center gap-1 text-sm font-bold text-[#f5b700]">{store.isVerified && <ShieldCheck size={16}/>}VERIFIED ESUT SELLER</p><h1 className="mt-2 text-3xl font-extrabold">{store.name}</h1><p className="mt-2 flex items-center gap-1 text-sm text-slate-300"><MapPin size={15}/>{store.location}</p></div></div><Button variant="outline" className="border-white/30 text-white hover:bg-white hover:text-[#111827]" onClick={() => reportItem("STORE", store.id)}><Flag size={16}/>Report store</Button></div>{store.description && <p className="mt-6 max-w-3xl leading-7 text-slate-200">{store.description}</p>}</section><section className="mt-8"><div className="flex items-end justify-between"><div><p className="eyebrow text-[#00843d]">STORE CATALOGUE</p><h2 className="mt-2 text-2xl font-extrabold">Active listings</h2></div><span className="text-sm text-slate-500">{products.length} available</span></div>{products.length ? <div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{products.map(({ listing, image }) => <Link key={listing.id} href={`/product/${listing.slug}`} className="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200 hover:-translate-y-0.5"><div className="product-visual aspect-[1/.75]">{image?.url ? <img src={image.url} alt={listing.title} className="h-full w-full object-cover"/> : <Package size={38}/>}</div><div className="p-4"><h3 className="line-clamp-2 font-extrabold">{listing.title}</h3><p className="mt-3 text-lg font-extrabold text-[#e31b23]">{naira(listing.priceKobo)}</p></div></Link>)}</div> : <div className="mt-5 rounded-2xl bg-white p-8 text-center text-slate-500 shadow-sm ring-1 ring-slate-200">This verified seller has no active listings at the moment.</div>}</section><section className="mt-10 max-w-3xl"><p className="eyebrow text-[#e31b23]">VERIFIED PURCHASES</p><h2 className="mt-2 text-2xl font-extrabold">Buyer reviews</h2>{reviews.length ? <div className="mt-5 space-y-4">{reviews.map(({ review, buyer, listing }) => <article key={review.id} className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex items-start justify-between gap-3"><div><p className="font-extrabold">{listing.title} · {"★".repeat(review.rating)}</p><p className="mt-1 text-sm text-slate-500">Verified buyer: {buyer.name ?? "Marketplace member"}</p></div><button type="button" className="text-xs font-bold text-slate-500 hover:text-[#e31b23]" onClick={() => reportItem("REVIEW", review.id)}>Report review</button></div><p className="mt-3 text-sm text-slate-700">{review.comment ?? "No written comment."}</p>{review.sellerResponse && <p className="mt-3 rounded-xl bg-[#eaf7ef] p-3 text-sm"><b>Seller response:</b> {review.sellerResponse}</p>}</article>)}</div> : <p className="mt-5 rounded-2xl bg-white p-6 text-sm text-slate-500 shadow-sm ring-1 ring-slate-200">There are no published verified-purchase reviews for this store yet.</p>}</section></div></main>; }
